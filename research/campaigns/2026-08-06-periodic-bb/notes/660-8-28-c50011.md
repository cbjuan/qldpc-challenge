# [[660,8,28]] — distinct high-distance BB mutation c50011

## Direction & hypothesis

Apply a one-support mutation to the trusted `[[660,8,34]]` parent on
`Z_30 x Z_11`, preserving `k=8` while probing neighboring high-distance
supports. Candidate c50011 advances weight-6 × unrestricted. Its distance is
a witness-backed upper bound; literature novelty is unverified.

## What was searched

The second validated-parent epoch covered 2,000 deterministic proposals at
IDs 49163--51162; its distance-first queue retained 12 connected candidates.
c50011 uses `validated-highd-local-one`, canonical key
`7b4f8e982b71177d9f15ecc1`, and proposal seed `314899940552997362`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(38,44)` at 400,
`(32,34)` at 8k, `(32,30)` at 60k, and `(28,30)` at 1M. All-rung
consolidation retained the ladder `38→32→30→28`, supporting `d<=28`, not an
exact distance. Trusted validation passed with seed `1507273980` and marked
the code board-advancing. `k^2*d/n=2.72`; board FOM `k*d^2/n=9.50`. The
support is structurally distinct but locally dominated by the campaign's
`[[660,8,34]]` points. Final artifact:
`c0050011-7b4f8e982b-validated-highd-epoch2-nonroot-final-allrungs-s1989818206.json`.

## Dead ends

The estimate dropped at every major allocation, from 38 to 28. This support
is useful structural evidence but not a new local parameter frontier.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_11` with `A=[(2,5),(8,1),(10,6)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c50011 uses
`validated-highd-local-one` with proposal seed `314899940552997362`.
