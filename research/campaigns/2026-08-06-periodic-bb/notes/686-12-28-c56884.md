# [[686,12,28]] — high-distance epoch-4 BB mutation c56884

## Direction & hypothesis

Use a two-step support mutation of the trusted `[[686,6,36]]` parent on
`Z_49 x Z_7`, seeking to increase encoded dimension without surrendering the
high-distance regime. Candidate c56884 is connected and advances weight-6 ×
unrestricted. Its distance is a witness-backed upper bound; literature
novelty is unverified.

## What was searched

Epoch 4 covered IDs 54663--57662: 3,000 deterministic proposals, 369 new
encoders, and 345 connected records. Eighteen connected children of
`parent_d>=28` were frozen for the distance-first screen. c56884 uses
`validated-highd-two-step`, canonical key `add1f28e9473301022dda49a`, and
proposal seed `11297879902218434326`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(48,48)` at 400,
`(34,34)` at 8k, `(34,30)` at 60k, and `(28,30)` at 1M. All-rung
consolidation retained the ladder `48→34→30→28`, supporting `d<=28`, not an
exact distance. Trusted validation passed with seed `1143973618` and marked
the code board-advancing. `k^2*d/n=5.88`; board FOM `k*d^2/n=13.71`. The
support is distinct but locally dominated by the smaller `[[576,12,28]]`.
Final artifact:
`c0056884-add1f28e94-epoch4-c56884-final-allrungs-s255505412.json`.

## Dead ends

The initial `d<=48` estimate fell at every deeper rung and ended 20 lower at
1M. The candidate did not improve the local `k=12,d=28` parameter frontier,
but its full ladder is useful calibration for mutations of the d=36 parent.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_49 x Z_7` with `A=[(3,3),(8,2),(11,0)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c56884 uses
`validated-highd-two-step` with proposal seed `11297879902218434326`.
