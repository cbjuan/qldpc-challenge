# [[648,8,30]] — refreshed high-distance BB mutation c52765

## Direction & hypothesis

Make a two-step support mutation of a trusted `Z_12 x Z_27` high-distance
parent, aiming to preserve `k=8` near the local distance ceiling. Candidate
c52765 is connected and advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound; literature novelty is unverified.

## What was searched

The refreshed epoch covered 3,000 deterministic proposals, producing 508
encoders and 476 connected records. c52765 uses
`validated-highd-two-step`, canonical key `7b86d1f56009bb49db843b57`, and
proposal seed `6592779300649082116`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(44,42)` at Python 400, `(32,36)` at
8k, `(30,36)` at 60k, and `(30,36)` at 1M. All-rung consolidation retained
the ladder `42→32→30→30`, supporting `d<=30`, not an exact distance. Trusted
validation passed with seed `449876947` and marked the code board-advancing.
`k^2*d/n=2.96`; board FOM `k*d^2/n=11.11`. The point ties the distinct local
c49503 `[[648,8,30]]` support and is locally dominated by the campaign's
unsubmitted `[[648,8,34]]` support. Final artifact:
`c0052765-7b86d1f560-epoch3-final-allrungs-s231361423.json`.

## Dead ends

The shallow estimate dropped by ten at 8k and by another two at 60k. A second
`Z_12 x Z_27`, `k=4` child, c52425, reached `d=34` at 8k but was not allocated
deep compute because c51880 was the stronger distance-ceiling exception.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_12 x Z_27` with `A=[(0,2),(0,7),(3,0)]` and
`B=[(0,3),(1,26),(2,25)]`; candidate c52765 uses
`validated-highd-two-step` with proposal seed `6592779300649082116`.
