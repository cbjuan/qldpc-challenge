# [[576,16,20]] — two-step high-distance BB mutation on Z_12 x Z_24

## Direction & hypothesis

Mutate the validated `[[576,12,28]]` BB parent while holding its rectangular
torus fixed, seeking more logical qubits without falling below the campaign's
high-distance regime. Candidate c50641 is connected and advances weight-6 ×
unrestricted. Its distance is a witness-backed upper bound, and literature
novelty is unverified.

## What was searched

The second validated-high-distance mutation epoch drew local, correlated, and
two-step proposals from trusted `d=12--36` parents. c50641 is a two-step child
of c45091, with canonical key `2d96a2074c021bc90cd5c488` and proposal seed
`17111150314898155896`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(24,24)` at Python 400 and `(20,20)`
at fresh Python/accelerated 8k, accelerated 60k, accelerated 1M, and final
all-rung consolidation. Thus the upper-bound ladder was `24→20→20→20` over
400→8k→60k→1M. The canonical document embeds explicit weight-20 X and Z
logicals; this supports `d<=20`, not an exact-distance claim. Trusted
validation passed with seed `1999062644` and marked the code board-advancing.
`k^2*d/n=8.89`; board FOM `k*d^2/n=11.11`. Artifact:
`c0050641-2d96a2074c-epoch2-early-final-allrungs-s752443071.json`.

## Dead ends

All five epoch-two early finalists passed the trusted gate, but the other four
were board-nonadvancing. Only c50641 was staged as a new trusted find.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_12 x Z_24` with `A=[(3,0),(10,2),(11,7)]` and
`B=[(0,3),(1,0),(2,0)]`; candidate c50641 uses
`validated-highd-two-step`.
