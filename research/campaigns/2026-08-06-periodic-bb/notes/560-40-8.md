# [[560,40,8]] — exact-support BB continuation on Z_20 x Z_14

## Direction & hypothesis

Search the exact-geometry continuation of the local `[[270,18,9]]` supports
for a shorter, higher-rate code that retains `d>=8`. The point advances
weight-6 × unrestricted; literature novelty is unverified.

## What was searched

Thirty-two connected exact-grid records were screened at Python-400. Candidate
c45046 has canonical key `8eb759b13183f688ae6d8ae8`; 28 records reached the
8k/60k/1M ladder.

## Evidence trail

The candidate remained at `d=8` for every 400→8k→60k→1M rung. Consolidation
selected weight-8 witnesses on both logical sides. The trusted validator passed
with seed `182832801` and marked it board-advancing. `k^2*d/n=22.86`; board FOM
`4.57`. Artifact:
`c0045046-8eb759b131-1m-python8000reuse8k-s1241659517-fast1000000-allrungs.json`.

## Dead ends

Four shallow records fell below eight (c45024=7, c45029=6, c45034=5,
c45038=7); no promoted record later collapsed.

## Tools

`campaign.py`, kit packaging, `gf2_fast` with `pair_depth=16` and four threads,
and trusted validation.

## Reproduction

Build on `Z_20 x Z_14` with `A=[(0,0),(0,11),(7,2),(7,8)]` and
`B=[(0,0),(0,1)]`; candidate c45046 is in the survivor selection.
