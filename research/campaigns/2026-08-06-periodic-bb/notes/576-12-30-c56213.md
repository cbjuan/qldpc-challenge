# [[576,12,30]] — high-distance/high-k BB sweet spot c56213

## Direction & hypothesis

Apply a two-step support mutation to the trusted `[[576,12,28]]` parent on
`Z_12 x Z_24`, explicitly seeking a distance increase without sacrificing
encoded dimension. Candidate c56213 is connected and advances weight-6 ×
unrestricted. Its distance is a witness-backed upper bound; literature
novelty is unverified.

## What was searched

Epoch 4 covered IDs 54663--57662: 3,000 deterministic proposals, 369 new
encoders, and 345 connected records. Eighteen connected children of
`parent_d>=28` were frozen for the distance-first screen. c56213 uses
`validated-highd-two-step`, canonical key `d2e5e445f5073e9e2650558f`, and
proposal seed `18253120098272623246`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(42,40)` at 400,
`(32,34)` at 8k and 60k, and `(30,34)` at 1M. All-rung consolidation
retained the ladder `40→32→32→30`, supporting `d<=30`, not an exact
distance. Trusted validation passed with seed `1964399631` and marked the
code board-advancing. `k^2*d/n=7.50`; board FOM `k*d^2/n=18.75`, the
strongest value among the campaign's validated `d>=8` periodic-BB finds.
Final artifact:
`c0056213-d2e5e445f5-epoch4-c56213-final-allrungs-s596989082.json`.

## Dead ends

The shallow estimate fell by ten over the full ladder, and the decisive
weight-30 X logical appeared only at 1M. Even after that correction, the
child improves its `[[576,12,28]]` parent's witnessed distance at fixed n and
k, making it the first epoch-4 result to improve the local sweet-spot
frontier itself.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_12 x Z_24` with `A=[(0,2),(1,9),(3,0)]` and
`B=[(0,3),(1,0),(2,0)]`; candidate c56213 uses
`validated-highd-two-step` with proposal seed `18253120098272623246`.
