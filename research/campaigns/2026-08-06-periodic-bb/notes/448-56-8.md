# [[448,56,8]] — compact high-rate weight-8 periodic BB

## Direction & hypothesis

Search support-preserving mutations of the validated d=8 periodic-BB lane for
a better size/rate sweet spot while enforcing witnessed `d >= 8` at every deep
promotion.

## What was searched

Candidate 45591 is a connected correlated support mutation on `Z_28 x Z_8`.
It was packaged at 400 Python RIS trials and promoted through fresh 8k, reused-
Python 60k, and reused-Python 1M rungs.

## Evidence trail

The witnessed distance remained 8 throughout the complete ladder. The 1M rung
used seed 1124229271 and persisted a weight-8 X witness. Trusted validation
passed at seed 355606558, found no lighter logical in 8,000 RIS trials, found
no exact or WL-equivalent board entry, and labels the code as advancing
weight-8 x unrestricted.

Claim: `d <= 8`, not exact; literature novelty unverified.

- Campaign priority: `k^2*d/n = 56.0000`.
- Board FOM: `k*d^2/n = 8.0000`.

## Dead ends

No distance collapse occurred through 1M. Several larger high-rate mutations
also stayed at d=8 but were dominated in their board cell.

## Tools

Periodic BB kit, deterministic campaign runner, `gf2_fast`, trusted Python
witness checks, and the local trusted validator.

## Reproduction

On `Z_28 x Z_8`, use
`A={(0,0),(0,5),(1,5),(1,7)}` and
`B={(0,0),(0,1),(5,1),(5,7)}`.
Candidate ID 45591; full artifacts are under `../artifacts/candidates/`.
