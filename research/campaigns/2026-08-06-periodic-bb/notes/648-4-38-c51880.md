# [[648,4,38]] — refreshed BB distance-ceiling mutation c51880

## Direction & hypothesis

Use a correlated mutation on `Z_12 x Z_27` to probe the distance ceiling even
when the encoded dimension falls to `k=4`. c51880 was the only low-k child
promoted deeply: its 8k witness exceeded every `k>=8` survivor by at least
six. It advances weight-6 × unrestricted. Distance is a witness-backed upper
bound; literature novelty is unverified.

## What was searched

The refreshed 3,000-proposal epoch yielded 508 encoders and 476 connected
records. Eighteen received shallow witnesses and eleven reached 8k. c51880
uses `validated-highd-correlated`, canonical key
`ee8ddb96a46ae9a0f16145f6`, and proposal seed `1309420540903913540`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(50,52)` at Python 400, `(42,44)` at
8k, `(40,44)` at 60k, and `(38,44)` at 1M. All-rung consolidation retained
the ladder `50→42→40→38`, supporting `d<=38`, not an exact distance. Trusted
validation passed with seed `212469878` and marked the code board-advancing.
`k^2*d/n=0.94`; board FOM `k*d^2/n=8.91`. This locally improves the
campaign's `[[648,4,36]]` parameter point. Final artifact:
`c0051880-ee8ddb96a4-epoch3-final-allrungs-s1593271430.json`.

## Dead ends

Two other `k=4` children reached `d=36` and `d=34` at 8k, but only c51880
met the distance-ceiling exception for deep compute. Its shallow estimate
still fell by twelve over the full ladder, reinforcing the 1M floor.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_12 x Z_27` with `A=[(0,2),(3,0),(11,8)]` and
`B=[(0,3),(1,0),(1,1)]`; candidate c51880 uses
`validated-highd-correlated` with proposal seed `1309420540903913540`.
