# [[576,16,20]] — fourth structurally distinct BB support

## Direction & hypothesis

Search the weight-6 × unrestricted cell inside the user-locked periodic
bivariate-bicycle family, prioritizing witnessed distance at least 8 while
retaining useful encoded dimension. Candidate c60754 is a connected two-step
mutation of the locally validated `[[576,16,20]]` c59571 support on the same
`12 × 24` torus. The goal was to test whether the useful `k=16`, `d=20`
parameter point persists under another support mutation.

## What was searched

Epoch 6 deterministically censused 3,300 proposals, IDs c60663–c63962: 589
encoded and 558 connected. A disjoint seven-record high-`k` balance queue was
frozen in `selection-refreshed-highk-balance-epoch6-001.json`. c60754 was a
parent-`d=20`, child-`k=16` selection with proposal seed
`8183133690286134536`, then screened at Python 400, fresh Python 8k, and
accelerated 8k, 60k, and 1M rungs.

## Evidence trail

The persisted side-aware witness ladder was:

- Python 400, seed `1742040325`: `(X,Z)=(20,20)`, hence `d<=20`;
- Python 8k, seed `34130476`: `(20,20)`;
- accelerated 8k, seed `34130493`: X-20, retaining `(20,20)`;
- accelerated 60k, seed `1826260024`: X-20, retaining `(20,20)`;
- accelerated 1M, seed `865097658`: X-20, retaining `(20,20)`;
- all-rung consolidation, seed `1169629506`: `(20,20)` from seven persisted
  submission documents.

The scalar distance ladder was flat at 20 from the first screen through 1M.
The final claim is the witness-backed upper bound `d <= 20`, not an exact
distance. The consolidated artifact embeds independently checked explicit X
and Z logicals, both of weight 20. Trusted validation passed with seed
`1831679841`, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent public-board entry, and labels the code as advancing the
weight-6 × unrestricted board. Literature novelty is unverified.

The support is structurally distinct but ties the existing local
`[[576,16,20]]` c50641, c52988, and c59571 results. It is the fourth campaign
support at these parameters and adds reproducibility and support diversity
rather than a new numerical Pareto point.

- Campaign FOM: `k^2*d/n = 8.8889`.
- Board FOM: `k*d^2/n = 11.1111`.
- Canonical artifact:
  `c0060754-cdb21794c5-epoch6-balance-final-allrungs-s1169629506.json`.

## Dead ends

No rung found a witness lighter than 20: both sides were already at 20 after
the 400-trial screen, and the fresh Python and accelerated searches remained
flat through 1M trials. The mutation therefore tied its parent instead of
improving the local parameters.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first
`campaign.py` runner, `research/kit/submit.py`, `gf2_fast`, trusted Python
witness checks, and `verify/validate_candidate.py`. The ladder used one
400-trial screen, one fresh Python-8k search, accelerated 8k + 60k + 1M at
pair depth 1 on four threads, and the trusted 8k-RIS gate.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_12 × Z_24`:

```text
A = {(0,7), (4,20), (11,3)}
B = {(0,3), (2,1), (2,23)}
```

Candidate ID c60754; canonical key
`cdb21794c5964f088493da21`; proposal kernel
`validated-highd-two-step`; parent `validated-c59571-576-16-20`. The final
all-rung document and adjacent full verdict are under
`research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`. The
collision-safe review copies are `research/candidates/576-16-20-c60754.json`
and `research/candidates/576-16-20-c60754.verdict.json`.
