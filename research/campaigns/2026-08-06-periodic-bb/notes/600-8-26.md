# [[600,8,26]] — nearby-geometry high-distance BB transfer on Z_30 x Z_10

## Direction & hypothesis

Transfer the support of a validated `d=34` parent to a nearby rectangular
torus, testing whether substantial witnessed distance survives while keeping
weight-6 checks and `k=8`. Candidate c49795 is connected and advances
weight-6 × unrestricted. Its distance is a witness-backed upper bound, and
literature novelty is unverified.

## What was searched

The second validated-high-distance mutation epoch included nearby-geometry
children of trusted `d=12--36` parents. c49795 descends from c46841, with
canonical key `1b8d395eeb8c8613cc885811` and proposal seed
`4642901126757104201`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(34,36)` at Python 400, `(30,26)` at
accelerated 8k, and `(26,26)` at both 60k and 1M. Final all-rung consolidation
retained `dX<=26` and `dZ<=26`, giving the upper-bound ladder
`34→26→26→26` over 400→8k→60k→1M. This supports `d<=26`, not an exact
distance. Trusted validation passed with seed `1629009368` and marked the code
board-advancing. `k^2*d/n=2.77`; board FOM `k*d^2/n=9.01`. Artifact:
`c0049795-1b8d395eeb-epoch2-highd-final-allrungs-s1731525563.json`.

## Dead ends

The 400-trial estimate of 34 fell to 26 at 8k. A lighter X witness then
appeared at 60k, balancing the final `(26,26)` evidence; 1M found no further
collapse.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_30 x Z_10` with `A=[(2,4),(8,1),(10,6)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c49795 uses
`validated-highd-nearby-geometry`.
