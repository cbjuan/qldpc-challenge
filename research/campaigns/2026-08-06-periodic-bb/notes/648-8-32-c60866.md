# [[648,8,32]] — distinct epoch-6 high-distance BB mutation c60866

## Direction & hypothesis

Search the weight-6 × unrestricted cell inside the user-locked periodic
bivariate-bicycle family, prioritizing witnessed distance at least 8 and then
useful encoded dimension. Candidate c60866 is a connected two-step mutation
of the locally validated `[[648,4,36]]` c45508 support on the same `12 × 27`
torus. The mutation doubled structural `k` to 8 while probing a second support
inside the parent's distance-ceiling basin.

## What was searched

Epoch 6 deterministically censused 3,300 proposals, IDs c60663–c63962: 589
encoded and 558 connected. Eighteen connected records were frozen in the
globally distance-prior/child-`k`-optimal
`selection-refreshed-highd-epoch6-001.json` under caps of two per parent, four
per geometry, and six per mutation kernel. c60866 was one of the two selected
parent-`d=36`, child-`k=8` records, with proposal seed
`11433030384059990931`. It was screened at Python 400, fresh Python 8k, and
accelerated 8k, 60k, and 1M rungs.

## Evidence trail

The persisted side-aware witness ladder was:

- Python 400, seed `1306421725`: `(X,Z)=(52,54)`, hence `d<=52`;
- Python 8k, seed `2131357516`: `(42,36)`, hence `d<=36`;
- accelerated 8k, seed `2131357533`: Z-36, retaining `(42,36)`;
- accelerated 60k, seed `2089339912`: raw Z-38, retaining the saved
  `(42,36)` witnesses;
- accelerated 1M, seed `1811561181`: X-32, giving `(32,36)`;
- all-rung consolidation, seed `1487922377`: `(32,36)` from seven persisted
  submission documents.

The final claim is the witness-backed upper bound `d <= 32`, not an exact
distance. The consolidated artifact embeds independently checked explicit X
and Z logicals of weights 32 and 36. Trusted validation passed with seed
`1849135652`, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent public-board entry, and labels the code as advancing the
weight-6 × unrestricted board. Literature novelty is unverified.

The support is structurally distinct, but this point is locally dominated by
the campaign's `[[648,8,34]]` supports with the same `n` and `k`. It advances
the public board because those stronger campaign results remain local.

- Campaign FOM: `k^2*d/n = 3.1605`.
- Board FOM: `k*d^2/n = 12.6420`.
- Canonical artifact:
  `c0060866-186ef035e1-epoch6-primary-final-allrungs-s1487922377.json`.

## Dead ends

The shallow estimate fell from 52 to 36 at 8k. The accelerated 60k search did
not improve the retained Z-36 witness, and 1M finally found X-32. The final
point doubles its parent's `k`, but it does not reach the local distance-34
frontier for `[[648,8,*]]` supports.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first
`campaign.py` runner, `research/kit/submit.py`, `gf2_fast`, trusted Python
witness checks, and `verify/validate_candidate.py`. The 8k accelerated rung
used pair depth 16 on eight threads; the 60k and 1M rungs used pair depth 1 on
four threads. The trusted gate added 8,000 RIS trials.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_12 × Z_27`:

```text
A = {(0,2), (0,7), (3,0)}
B = {(0,3), (1,25), (5,2)}
```

Candidate ID c60866; canonical key
`186ef035e101af27dd3a4886`; proposal kernel
`validated-highd-two-step`; parent `validated-c45508-648-4-36`. The final
all-rung document and adjacent full verdict are under
`research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`. The
collision-safe review copies are `research/candidates/648-8-32-c60866.json`
and `research/candidates/648-8-32-c60866.verdict.json`.
