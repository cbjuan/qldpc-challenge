# [[648,8,28]] — distinct two-step high-distance BB mutation c49803

## Direction & hypothesis

Apply a two-step support mutation to the validated `[[648,8,34]]` parent,
seeking another connected high-distance code with useful `k=8`. Candidate
c49803 advances the public weight-6 × unrestricted board. It is locally
dominated by the campaign's `[[648,8,30/34]]` supports, its distance is a
witness-backed upper bound, and literature novelty is unverified.

## What was searched

The second validated-high-distance mutation epoch sampled trusted
`d=12--36` parents with several support-preserving kernels. c49803 is a
two-step child of c45088 with canonical key `b5f292b63a3e6b1eb06d1c61` and
proposal seed `7891468291488589552`.

## Evidence trail

Python 400 already persisted a `(dX,dZ)=(28,54)` witness pair. The standalone
8k documents found `(32,30)` but did not erase the earlier weight-28 logical;
60k and 1M then found `(28,28)`. Final all-rung consolidation independently
checked every saved document and therefore retained the honest cumulative
upper-bound ladder `28→28→28→28` over 400→8k→60k→1M. This supports `d<=28`,
not an exact distance. Trusted validation passed with seed `93190436`, found
no exact or WL-equivalent board entry, and marked the candidate
board-advancing. `k^2*d/n=2.77`; board FOM `k*d^2/n=9.68`. Artifact:
`c0049803-b5f292b63a-epoch2-001-final-allrungs-s1186826014.json`.

## Dead ends

The candidate does not improve the campaign's local `n=648, k=8` distance
frontier. The apparently higher standalone 8k value was never a stronger
claim because the persisted 400-rung logical already proved `d<=28`; this is
why only fresh all-rung consolidated documents are validated.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_12 x Z_27` with `A=[(0,7),(4,0),(11,2)]` and
`B=[(0,3),(1,0),(2,0)]`; candidate c49803 uses
`validated-highd-two-step`.
