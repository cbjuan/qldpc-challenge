# [[630,4,36]] — epoch-4 distance-ceiling BB mutation c55406

## Direction & hypothesis

Apply a correlated support mutation to the trusted `[[630,8,30]]` parent on
`Z_15 x Z_21`, accepting a possible rate loss to probe the distance ceiling at
fixed block length. Candidate c55406 is connected and advances weight-6 ×
unrestricted. Its distance is a witness-backed upper bound; literature novelty
is unverified.

## What was searched

Epoch 4 covered IDs 54663--57662: 3,000 deterministic proposals, 369 new
encoders, and 345 connected records. Eighteen connected children of
`parent_d>=28` were frozen for the distance-first screen. c55406 uses
`validated-highd-correlated`, canonical key `60eb66039657c8c8e94e44e4`, and
proposal seed `9495427201904705996`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(50,58)` at 400, `(42,44)`
at 8k, and `(36,44)` at both 60k and 1M. All-rung consolidation retained the
ladder `50→42→36→36`, supporting `d<=36`, not an exact distance. Trusted
validation passed with refutation seed `235445764` and marked the code
board-advancing. `k^2*d/n=0.91`; board FOM `k*d^2/n=8.23`. This improves the
campaign's prior `[[630,4,32]]` point and establishes its `n=630,k=4` distance
ceiling. Final artifact:
`c0055406-60eb660396-epoch4-ceiling-final-allrungs-s1367341703.json`.

## Dead ends

The mutation traded half the parent's encoded dimension for distance, and the
400-trial estimate was 14 above the consolidated witness. The decisive
weight-36 X logical appeared by 60k; the flat 1M rung supports retaining this
as a distance-ceiling search parent, but does not make the distance exact.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, Python 400/8k witness searches, `gf2_fast` at `pair_depth=16` with
eight threads for 8k/60k and at `pair_depth=1` with four threads for 1M,
all-rung consolidation, and the pinned trusted validator.

## Reproduction

Build on `Z_15 x Z_21` with `A=[(0,2),(0,4),(4,1)]` and
`B=[(0,1),(0,6),(7,0)]`; candidate c55406 uses
`validated-highd-correlated` with proposal seed `9495427201904705996`.
