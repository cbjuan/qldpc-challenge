# [[540,8,26]] — local-pair high-distance BB mutation on Z_30 x Z_9

## Direction & hypothesis

Apply a paired local support mutation to the validated `[[540,8,30]]` parent,
testing whether the same torus can retain a large witnessed distance under a
structurally distinct sparse support. Candidate c49482 is connected and
advances weight-6 × unrestricted. Its distance is a witness-backed upper
bound, and literature novelty is unverified.

## What was searched

The second validated-high-distance mutation epoch drew local, correlated,
two-step, and nearby-geometry proposals from trusted `d=12--36` parents.
c49482 is a local-pair child of c46282 with canonical key
`ab6e196fc125bea31d474b31` and proposal seed `9416084196752018437`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(32,28)` at Python 400 and `(26,28)`
at accelerated 8k, 60k, and 1M. Final all-rung consolidation retained
`dX<=26` and `dZ<=28`, giving the upper-bound ladder `28→26→26→26` over
400→8k→60k→1M. This supports `d<=26`, not an exact-distance claim. The
trusted validator passed with seed `1020572113` and marked the code
board-advancing. `k^2*d/n=3.08`; board FOM `k*d^2/n=10.01`. Artifact:
`c0049482-ab6e196fc1-epoch2-highd-final-allrungs-s1452118083.json`.

## Dead ends

The 400-trial minimum of 28 fell to 26 at 8k, then remained stable through
1M. All three epoch-two high-distance finalists passed and advanced; the
shallow inflation was the only collapse in this candidate's ladder.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_30 x Z_9` with `A=[(2,4),(8,2),(11,8)]` and
`B=[(4,0),(6,3),(7,5)]`; candidate c49482 uses
`validated-highd-local-pair`.
