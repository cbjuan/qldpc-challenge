# [[108,36,3]] — high-rate weight-16 periodic BB

## Direction & hypothesis

Unrestricted periodic rectangular-torus bivariate-bicycle search with arbitrary
support sizes and weights. This candidate tests whether a shared-factor kernel
can retain distance above two while providing a high rate.

## What was searched

It arose in the first 20,000-proposal structural census. Of 8,449 encoding
codes retained, this was one of four stratified calibration candidates promoted
to a persisted 2,000-trial Python RIS witness search.

## Evidence trail

The saved search found `d <= 3`. Trusted validation passed, found no lighter
logical in 6,820 RIS trials at seed 1081654670, and labeled it as advancing the
weight-9plus × unrestricted board. The claim is an upper bound, not exact;
literature novelty is unverified.

## Dead ends

The same calibration batch's more extreme weight-6 and weight-8 factor codes
both collapsed to distance two. `[[40,2,4]]` passed but was dominated.

## Tools

Periodic `research/kit/bb.py`, the shared GF(2) core, persistence-first campaign
runner, and the trusted local validator. No external submission tooling.

## Reproduction

Build on `Z_3 x Z_18` with
`A={(0,0),(0,7),(0,12),(1,1),(1,2),(1,6),(2,6),(2,14)}` and
`B={(0,0),(0,12),(0,15),(1,13),(2,0),(2,3),(2,6),(2,7)}`.
The full submission and verdict are under `../artifacts/candidates/` with
candidate ID 2359.
