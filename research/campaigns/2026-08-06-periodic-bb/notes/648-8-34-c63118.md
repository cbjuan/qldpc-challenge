# [[648,8,34]] — distinct epoch-6 BB mutation c63118

## Direction & hypothesis

Apply a two-step support mutation to the trusted `[[648,8,32]]` c58638
parent on `Z_12 x Z_27`, seeking to recover the geometry's local `d=34`
ceiling while preserving `k=8`. Candidate c63118 is connected and advances
weight-6 × unrestricted. Its distance is a witness-backed upper bound;
literature novelty is unverified.

## What was searched

Epoch 6 covered IDs 60663--63662: 3,000 deterministic proposals, 552 new
encoders, and 523 connected records. Eighteen connected children were frozen
for the distance-first screen. c63118 uses `validated-highd-two-step`,
canonical key `b3bed9d14306c192a595d890`, and proposal seed
`3657193864149011691`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(46,52)` at 400,
`(40,40)` at 8k, `(36,40)` at 60k, and `(34,40)` at 1M. All-rung
consolidation retained the ladder `46→40→36→34`, supporting `d<=34`, not an
exact distance. Trusted validation passed with seed `217146722` and marked
the code board-advancing. `k^2*d/n=3.36`; board FOM `k*d^2/n=14.27`. The
support is a structurally distinct tie with c45088 at `[[648,8,34]]`, but is
locally dominated by the campaign's smaller `[[630,8,34]]` c50584 point.
Final artifact:
`c0063118-b3bed9d143-epoch6-final-allrungs-s690393143.json`.

## Dead ends

The shallow `d=46` signal fell at every deeper stage, and the decisive X=34
logical appeared only at 1M. Although the child improved on its c58638
parent's witnessed `d=32`, it did not create a new local parameter point or
improve the campaign Pareto frontier.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=1`, four threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_12 x Z_27` with `A=[(0,2),(0,7),(3,0)]` and
`B=[(1,2),(2,0),(9,1)]`; candidate c63118 uses
`validated-highd-two-step` with proposal seed `3657193864149011691`.
