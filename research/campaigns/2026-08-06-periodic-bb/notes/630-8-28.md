# [[630,8,28]] — high-distance local BB mutation on Z_45 x Z_7

## Direction & hypothesis

Perturb a calibrated sparse BB support on `Z_45 x Z_7`, seeking a connected
weight-6 code with useful `k=8` and a substantially larger witnessed distance
than the campaign floor. Candidate c48911 advances weight-6 × unrestricted.
Its distance is a witness-backed upper bound, and literature novelty is
unverified.

## What was searched

The high-distance continuation lane screened local and correlated mutations
around `d>=14` parent geometries. c48911 uses
`high-distance-local-step`, canonical key `70f214640c9262c9476079cc`, and
proposal seed `17990280529294541293`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(50,42)` at Python 400, `(36,34)` at
accelerated 8k, `(32,34)` at 60k, and `(32,28)` at 1M. Final all-rung
consolidation retained `dX<=32` and `dZ<=28`, giving the upper-bound ladder
`42→34→32→28` over 400→8k→60k→1M. This supports `d<=28`, not an exact
distance. Trusted validation passed with seed `1527923485` and marked the code
board-advancing. `k^2*d/n=2.84`; board FOM `k*d^2/n=9.96`. Artifact:
`c0048911-70f214640c-continuation-final-allrungs-s283679173.json`.

## Dead ends

The shallow estimate fell at every deeper rung, ending with a lighter Z
logical at 1M. Four of seven trusted continuation finalists were
board-nonadvancing; only the three advancing points were checkpointed.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_45 x Z_7` with `A=[(2,4),(8,2),(10,6)]` and
`B=[(3,0),(6,4),(7,5)]`; candidate c48911 uses
`high-distance-local-step`.
