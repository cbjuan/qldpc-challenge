# [[630,8,28]] — distinct epoch-4 BB mutation c54911

## Direction & hypothesis

Apply a paired local mutation to the trusted `[[630,8,30]]` parent on
`Z_15 x Z_21`, seeking another high-distance `k=8` support. Candidate c54911
is connected and advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound; literature novelty is unverified.

## What was searched

Epoch 4 covered IDs 54663--57662: 3,000 deterministic proposals, 369 new
encoders, and 345 connected records. Eighteen connected children of
`parent_d>=28` were frozen for the distance-first screen. c54911 uses
`validated-highd-local-pair`, canonical key
`e93ff10b704e62dc00e69eaa`, and proposal seed `9381082087678390251`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(30,34)` at 400,
`(30,30)` at 8k, and `(28,30)` at both 60k and 1M. All-rung consolidation
retained the ladder `30→30→28→28`, supporting `d<=28`, not an exact
distance. Trusted validation passed with seed `1444441646` and marked the
code board-advancing. `k^2*d/n=2.84`; board FOM `k*d^2/n=9.96`. The support
is structurally distinct but locally dominated by the campaign's
`[[630,8,30/34]]` points. Final artifact:
`c0054911-e93ff10b70-epoch4-primary-final-allrungs-s1213413907.json`.

## Dead ends

The 400 and 8k rungs stayed at 30, but the independent 60k rung found a
weight-28 X logical. The 1M rung confirmed that lower witness, so this
mutation did not improve its `[[630,8,30]]` parent locally.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_15 x Z_21` with `A=[(0,4),(2,2),(3,0)]` and
`B=[(0,6),(7,0),(13,0)]`; candidate c54911 uses
`validated-highd-local-pair` with proposal seed `9381082087678390251`.
