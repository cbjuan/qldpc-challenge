# [[576,12,28]] — long-axis exact transfer of the known d=18 BB support

## Direction & hypothesis

Extend the known `[[288,12,18]]` periodic bivariate-bicycle support along one
axis while preserving its six monomials.  The `Z_12 x Z_24` point offers a
moderate-rate test of high-distance persistence in the weight-6 track.

## What was searched

Candidate c45091 belonged to the bounded 18-point parent-`d>=14`
exact-geometry grid.  Every point was persisted at Python 400; 17 with a
witness of at least 8 entered fresh Python/accelerated 8k, accelerated 60k,
and accelerated 1M rungs.  The deeper accelerated rungs reused the saved
Python-8k base.  This code is connected.

## Evidence trail

The persisted `(X,Z)` witness weights were:

- Python 400: `(42,42)`;
- Python 8k and accelerated 8k: `(28,34)`;
- accelerated 60k: `(28,28)`;
- accelerated 1M: `(28,34)` raw using seed 1397454915;
- all-rung consolidation: `(28,28)`, retaining the lighter saved Z witness.

The final claim is `d <= 28` with `confidence: upper_bound`, not `d=28`.
Explicit weight-28 X and Z logicals are embedded in the canonical document.
Trusted validation passed with seed 73830804, found no lighter logical in
8,000 RIS trials, found no exact or WL-equivalent board entry, and labels the
code as advancing weight-6 x unrestricted.  Literature novelty is unverified.

- Campaign FOM: `k^2*d/n = 7.0000`.
- Board FOM: `k*d^2/n = 16.3333`.
- Canonical artifact: `c0045091-82d17b794e-grid-final-allrungs-s323119392.json`.

## Dead ends

The shallow distance estimate fell from 42 to 28 at 8k.  The independent 1M
run did not recover the Z-28 witness found at 60k, so the final consolidation
had to preserve the lightest validated witness across all rungs.  Grid sibling
c45110 collapsed to distance 4.

## Tools

Codex agent harness; periodic-BB constructor, persistence-first campaign
runner, submission packager, `gf2_fast`, trusted Python witness checks, and
the trusted validator.  Compute comprised Python 400 and fresh Python 8k plus
8k, 60k, and 1M accelerated trials.

## Reproduction

Build the periodic BB code on `Z_12 x Z_24` with:

```text
A = {(0,2), (0,7), (3,0)}
B = {(0,3), (1,0), (2,0)}
HX = [A | B], HZ = [B^T | A^T]
```

Candidate ID c45091; canonical key
`82d17b794e12ad25950bb24e`.  The canonical all-rung document and verdict are
under `research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`.
