# [[630,8,34]] — high-distance BB mutation c50584

## Direction & hypothesis

Mutate the trusted `[[630,8,30]]` parent on `Z_15 x Z_21` while preserving
`k=8`, seeking a better high-distance/high-k balance. Candidate c50584 is
connected and advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound; literature novelty is unverified.

## What was searched

The second validated-parent epoch structurally censused 2,000 deterministic
proposals at IDs 49163--51162. The distance-first epoch-2 queue retained 12
connected candidates. c50584 uses `validated-highd-two-step`, canonical key
`6319d85735ce46aa7d307896`, and proposal seed `11592882522982140223`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(54,38)` at 400,
`(40,38)` at 8k, `(38,38)` at 60k, and `(38,34)` at 1M. All-rung
consolidation retained the ladder `38→38→38→34`, supporting `d<=34`, not an
exact distance. Trusted validation passed with seed `457263404` and marked
the code board-advancing. `k^2*d/n=3.45`; board FOM `k*d^2/n=14.68`, the
strongest local value in the campaign's `k=8`, `d>=30` cluster. Final
artifact: `c0050584-6319d85735-validated-highd-epoch2-nonroot-final-allrungs-s1192805705.json`.

## Dead ends

The apparent flat `d=38` value through 60k was not converged: a fresh 1M
rung found the decisive weight-34 Z logical. This is another direct example
of why the large-code 1M floor remains mandatory.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_15 x Z_21` with `A=[(0,2),(0,4),(3,0)]` and
`B=[(0,6),(7,0),(14,18)]`; candidate c50584 uses
`validated-highd-two-step` with proposal seed `11592882522982140223`.
