# [[208,26,6]] — balanced weight-6 periodic BB

## Direction & hypothesis

Target the empty high-rate branch of the unrestricted weight-6 frontier at
`145 <= n <= 287`, where even `k >= 26` is automatically nondominated.

## What was searched

20,000 deterministic proposals spanning arbitrary periodic rectangular-torus
BB supports. This candidate came from the axis-mixed kernel on a balanced
`8x13` torus and survived connectedness and exact-k screening.

## Evidence trail

The witnessed distance stayed at 6 across saved 2k, 8k, and 60k rungs. The
60k rung used an 8,000-trial Python witness pass plus a 60,000-trial accelerated
pass at seed 730123211; the accelerated witness passed the trusted Python GF(2)
checks. Trusted validation at seed 2003193702 passed, found no lighter logical
in its independent 8,000-trial pass, and labeled the code board-advancing.
Claim: witness-backed `d <= 6`, not exact. Literature novelty unverified.

## Dead ends

Extreme common-factor codes in the same census collapsed to `d=2`; this
balanced non-factor geometry was retained while those kernels were down-weighted.

## Tools

`research/kit/bb.py`, `gf2_fast`, the Python GF(2) witness checks, and
`verify/validate_candidate.py`. All artifacts remain local.

## Reproduction

On `Z_8 x Z_13`, use
`A={(0,0),(0,7),(2,7),(5,0)}` and `B={(0,0),(1,0)}`.
Candidate ID 16229; full saved candidate, raw accelerated evidence, and verdict
are under `../artifacts/candidates/`.
