# [[648,8,28]] — distinct epoch-4 BB mutation c55020

## Direction & hypothesis

Use a two-step mutation of a trusted `[[648,4,36]]` parent on
`Z_12 x Z_27`, seeking to gain encoded dimension while retaining useful high
distance. Candidate c55020 is connected and advances weight-6 × unrestricted.
Its distance is a witness-backed upper bound; literature novelty is
unverified.

## What was searched

Epoch 4 covered IDs 54663--57662: 3,000 deterministic proposals, 369 new
encoders, and 345 connected records. Eighteen connected children of
`parent_d>=28` were frozen for the distance-first screen. c55020 uses
`validated-highd-two-step`, canonical key `3fdd138b51b784ebe7abdae6`, and
proposal seed `3912412903754629818`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(40,30)` at 400,
`(30,34)` at 8k, and `(28,30)` at both 60k and 1M. All-rung consolidation
retained the ladder `30→30→28→28`, supporting `d<=28`, not an exact
distance. Trusted validation passed with seed `1799729346` and marked the
code board-advancing. `k^2*d/n=2.77`; board FOM `k*d^2/n=9.68`. The support
is distinct but locally dominated by the campaign's `[[648,8,30/34]]`
points. Final artifact:
`c0055020-3fdd138b51-epoch4-primary-final-allrungs-s2055755572.json`.

## Dead ends

The candidate doubled the parent's encoded dimension from 4 to 8, but its
witnessed distance settled eight below the parent's 36 and did not improve
the local `Z_12 x Z_27` frontier.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_12 x Z_27` with `A=[(0,2),(0,7),(3,0)]` and
`B=[(0,3),(4,3),(11,0)]`; candidate c55020 uses
`validated-highd-two-step` with proposal seed `3912412903754629818`.
