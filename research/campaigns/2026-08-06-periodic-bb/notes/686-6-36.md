# [[686,6,36]] — high-distance local BB mutation on Z_49 x Z_7

## Direction & hypothesis

Mutate a calibrated high-distance support on a long rectangular torus,
accepting lower logical dimension in exchange for a larger witnessed distance.
Candidate c48487 is connected and advances weight-6 × unrestricted. Its
distance is a witness-backed upper bound, and literature novelty is unverified.

## What was searched

The continuation lane explored sparse local and correlated mutations around
`d>=14` parent geometries. c48487 uses `high-distance-local-step`, canonical
key `c58b080116488e2ecc27f767`, and proposal seed
`7536982447224508289`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(54,50)` at Python 400, `(52,48)` at
accelerated 8k, `(46,48)` at 60k, and `(36,48)` at 1M. Final all-rung
consolidation retained `dX<=36` and `dZ<=48`, so the upper-bound ladder was
`50→48→46→36` over 400→8k→60k→1M. This supports `d<=36`, not an exact
distance. Trusted validation passed with seed `2028378699` and marked the code
board-advancing. `k^2*d/n=1.89`; board FOM `k*d^2/n=11.34`. Artifact:
`c0048487-c58b080116-continuation-final-allrungs-s1030785542.json`.

## Dead ends

Each deeper rung found a lighter X logical, with the largest drop arriving at
1M (`46→36`). Four of seven trusted continuation finalists did not advance
their board cell and remain outside this checkpoint.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_49 x Z_7` with `A=[(2,4),(8,2),(11,0)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c48487 uses
`high-distance-local-step`.
