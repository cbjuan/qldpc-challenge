# [[432,68,8]] — high-rate exact-support BB continuation on Z_27 x Z_8

## Direction & hypothesis

Extend the local `[[272,48,8]]` exact supports across a longer rectangular
torus, retaining `d>=8` while increasing `k^2*d/n`. This candidate advances
weight-8 × unrestricted; literature novelty is unverified.

## What was searched

Thirty-two connected exact-geometry records were screened at Python-400 in
`selection-016.json`. Candidate c45032 has canonical key
`0c963b8208e97c1b6ae40092` and was one of 28 `d>=8` survivors.

## Evidence trail

The ladder was flat at `d=8` for 400→8k→60k→1M. The all-rung candidate
contains weight-8 X and Z witnesses after comparing every persisted rung. The
trusted gate passed with seed `2116010245` and reported board advancement.
`k^2*d/n=85.63`; board FOM `10.07`. Artifact:
`c0045032-0c963b8208-1m-python8000reuse8k-s377109834-fast1000000-allrungs.json`.

## Dead ends

Four Python-400 records collapsed below the floor: c45024 (`7`), c45029 (`6`),
c45034 (`5`), and c45038 (`7`). The other 28 stayed flat through 1M.

## Tools

`campaign.py`, kit submission helpers, `gf2_fast` (`pair_depth=16`, four
threads), and the trusted validator.

## Reproduction

Use `Z_27 x Z_8`, `A=[(0,0),(0,5),(1,0),(1,5)]`, and
`B=[(0,0),(0,1),(5,0),(5,1)]`; rebuild c45032 from
`selection-highd-exact-rate-001-survivors.json`.
