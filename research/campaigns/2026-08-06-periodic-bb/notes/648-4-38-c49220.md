# [[648,4,38]] — distance-ceiling BB mutation c49220

## Direction & hypothesis

Mutate the trusted `[[648,4,36]]` periodic-BB parent on `Z_12 x Z_27` to
probe the reachable distance ceiling. Candidate c49220 remains connected and
advances weight-6 × unrestricted. Its distance is a witness-backed upper
bound; literature novelty is unverified.

## What was searched

The second validated-parent epoch structurally censused 2,000 deterministic
proposals at IDs 49163--51162. Its first promotion file contained 32 connected
candidates, 30 of which remained in the survivor queue. c49220 uses the
`validated-highd-correlated` kernel, canonical key
`a008d53cd9b2228f412f7299`, and proposal seed `4454297517569839279`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(64,46)` at Python 400, `(46,40)` at
8k, `(40,40)` at 60k, and `(38,40)` at 1M. All-rung consolidation retained
the ladder `46→40→40→38`, supporting `d<=38`, not an exact distance. Trusted
validation passed with seed `302359288` and marked the code board-advancing.
`k^2*d/n=0.94`; board FOM `k*d^2/n=8.91`. It ties the structurally distinct
c51880 support at the campaign's current local distance ceiling. Final
artifact: `c0049220-a008d53cd9-epoch2-001-final-allrungs-s345672872.json`.

## Dead ends

The shallow distance estimate fell by eight over the full ladder, and the
decisive X logical appeared only at 1M. The retained dimension is `k=4`, so
future allocation prioritizes mutations that preserve more logical qubits.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_12 x Z_27` with `A=[(0,7),(1,3),(3,0)]` and
`B=[(0,3),(2,1),(4,2)]`; candidate c49220 uses
`validated-highd-correlated` with proposal seed `4454297517569839279`.
