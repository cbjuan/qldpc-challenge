# [[660,8,28]] — distinct local-one high-distance BB mutation c49291

## Direction & hypothesis

Apply a one-term local support mutation to the validated `[[660,8,34]]`
parent, seeking another connected high-distance code with useful `k=8`.
Candidate c49291 advances the public weight-6 × unrestricted board. It is
locally dominated by the campaign's `[[660,8,34]]` supports, its distance is
a witness-backed upper bound, and literature novelty is unverified.

## What was searched

The second validated-high-distance mutation epoch sampled trusted
`d=12--36` parents with several support-preserving kernels. c49291 is a
local-one child of c46841 with canonical key `93ec4d28ac16bb392a9ebae7` and
proposal seed `6079791922688247859`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(38,44)` at Python 400, `(30,32)` at
accelerated 8k, `(28,32)` at 60k, and `(28,32)` at 1M. Final all-rung
consolidation retained `dX<=28` and `dZ<=32`, giving the upper-bound ladder
`38→30→28→28` over 400→8k→60k→1M. This supports `d<=28`, not an exact
distance. Trusted validation passed with seed `1652710357`, found no exact or
WL-equivalent board entry, and marked the candidate board-advancing.
`k^2*d/n=2.72`; board FOM `k*d^2/n=9.50`. Artifact:
`c0049291-93ec4d28ac-epoch2-001-final-allrungs-s679884003.json`.

## Dead ends

The candidate descended by ten from its shallow value and does not improve
the campaign's local `n=660, k=8` distance frontier. It is retained because
the trusted gate independently marked this distinct support as advancing the
public board.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_30 x Z_11` with `A=[(2,6),(8,1),(10,6)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c49291 uses
`validated-highd-local-one`.
