# [[234,26,7]] — weight-6 BB geometry transfer on a 9x13 torus

## Direction & hypothesis

Search nearby geometries of the local `[[208,26,6]]` support pattern for a
short, higher-distance point on the unrestricted weight-6 frontier.

## What was searched

The 20,000-proposal broad structural census was followed by 20,000 targeted
parent mutations. Candidate 20918 is a connected nearby-geometry child in the
32-record promotion batch, initially packaged with 400 Python witness trials.
It preserves the 4+2 supports of the `8x13` parent while extending the first
torus dimension to 9.

## Evidence trail

The lightest X and Z witnesses both had weight 7 in the 400-trial package, and
the result stayed flat through the saved 8k and 60k confirmation rungs. The
final rung combined 8,000 Python trials at seed 1591394722 with 60,000
accelerated trials at seed 1591394739. The raw weight-7 X witness passed the
trusted Python GF(2) checks. Trusted validation at seed 175740810 passed, found
no lighter logical in 8,000 independent trials, found no exact or WL board
match, and labeled the code board-advancing.

Claim: witness-backed `d <= 7`, not exact. Literature novelty unverified.

FOMs: `k*d^2/n = 26*49/234 = 5.44`; `k^2*d/n = 676*7/234 = 20.22`.

## Dead ends

The same supports on `Z_10 x Z_13` produced the longer `[[260,26,6]]`, which
is locally dominated by this candidate. Smaller first dimensions gave the
sequence `[[156,26,4]]`, `[[182,26,5]]`, and `[[208,26,6]]`; the `9x13`
geometry continued that distance trend without changing `k`.

## Tools

The local campaign runner, periodic BB and submission kits, `gf2_fast` with
pair depth 16, trusted Python GF(2) witness checks, and the trusted local
validator. All outputs remain in the local campaign.

## Reproduction

On `Z_9 x Z_13`, use
`A={(0,0),(0,7),(2,7),(5,0)}` and `B={(0,0),(1,0)}`.
Candidate ID 20918; the final submission, evidence, and verdict are under
`../artifacts/candidates/`.
