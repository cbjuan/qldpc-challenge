# [[648,4,34]] — high-distance parent mutation on Z_27 x Z_12

## Direction & hypothesis

Mutate a high-distance BB parent locally while preserving its sparse weight-6
checks, aiming for a witnessed distance well above the campaign's `d>=8` floor.
Candidate c45244 advances weight-6 × unrestricted. The claim is an upper bound,
and literature novelty is unverified.

## What was searched

The `parent14` high-distance-transfer lane applied local and correlated support
steps to known high-distance parents. c45244 uses proposal
`high-distance-local-step` and canonical key
`e5dffae5b27e89f709fb5ce0`.

## Evidence trail

The witness upper bound tightened `52→40→36→34` across
Python-400→fast-8k→fast-60k→fast-1M. The final all-rung document has
`dX<=34` and `dZ<=40`. The trusted gate passed with seed `191029800` and marked
the code board-advancing. `k^2*d/n=0.84`; board FOM `k*d^2/n=7.14`. Artifact:
`c0045244-e5dffae5b2-parent14-final-allrungs-s1284160284.json`.

## Dead ends

All 19 final parent14 candidates passed the gate, but 11 did not advance their
board cell. Only the eight advancing points were staged.

## Tools

Persistence-safe campaign packaging, `gf2_fast` at pair depth 16 with four
threads, final all-rung witness consolidation, and the pinned trusted validator.

## Reproduction

Build on `Z_27 x Z_12` with `A=[(0,2),(2,5),(3,0)]` and
`B=[(0,6),(7,0),(14,0)]`; candidate ID c45244, kernel
`high-distance-local-step`.
