# [[648,8,30]] — distinct mutation on a high-distance BB geometry

## Direction & hypothesis

Search the weight-6 × unrestricted cell inside the user-locked periodic
bivariate-bicycle family, prioritizing witnessed distance at least 8 and then
useful encoded dimension. Candidate c58472 is a connected two-step mutation
of the locally validated `[[648,4,36]]` c45508 support on the same `12 × 27`
torus. The mutation doubled structural `k` from 4 to 8 while using the
parent's distance only as a scheduling prior.

## What was searched

Epoch 5 deterministically censused 3,000 proposals, IDs c57663–c60662. It
produced 440 encoders, of which 419 were connected. Eighteen connected
records were frozen in `selection-refreshed-highd-epoch5-001.json` under caps
of two per parent, four per geometry, and six per mutation kernel. c58472 was
selected from the parent-`d=36` layer with proposal seed
`14221044398159941276`, then screened at Python 400, fresh Python 8k, and
accelerated 8k, 60k, and 1M rungs.

## Evidence trail

The persisted side-aware witness ladder was:

- Python 400, seed `1263544317`: `(X,Z)=(44,32)`, hence `d<=32`;
- Python 8k, seed `2109339271`: raw `(38,34)`;
- accelerated 8k, seed `2109339288`: raw X-36, giving `(36,34)`; merging the
  lighter saved Z-32 witness retained cumulative `(36,32)`;
- accelerated 60k, seed `823199268`: X-30, giving `(30,32)`;
- accelerated 1M, seed `1773702519`: X-30, retaining `(30,32)`;
- all-rung consolidation, seed `1226758122`: `(30,32)` from seven persisted
  submission documents.

The final claim is the witness-backed upper bound `d <= 30`, not an exact
distance. The consolidated artifact embeds independently checked explicit X
and Z logicals of weights 30 and 32. Trusted validation passed with seed
`1278161614`, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent public-board entry, and labels the code as advancing the
weight-6 × unrestricted board. Literature novelty is unverified.

This support is structurally distinct, but locally it is dominated by the
campaign's unsubmitted `[[648,8,34]]` result with the same `n` and `k`.
Public-board advancement and local Pareto status are therefore different
statements.

- Campaign FOM: `k^2*d/n = 2.9630`.
- Board FOM: `k*d^2/n = 11.1111`.
- Canonical artifact:
  `c0058472-bb2c3fe1d7-epoch5-primary-final-allrungs-s1226758122.json`.

## Dead ends

The standalone Python-8k document appeared to improve the shallow estimate
from 32 to 34 only because it had not rediscovered the persisted Z-32 logical.
The all-source merge correctly retained that earlier witness, and the 60k rung
then found X-30. This is a concrete example of why promotion must merge every
persisted side rather than compare only scalar rung outputs.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first
`campaign.py` runner, `research/kit/submit.py`, `gf2_fast`, trusted Python
witness checks, and `verify/validate_candidate.py`. Per code the ladder used
one 400-trial screen, one fresh Python-8k search, accelerated 8k + 60k + 1M
searches at pair depth 1 on four threads, and the trusted 8k-RIS gate.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_12 × Z_27`:

```text
A = {(0,2), (0,7), (3,0)}
B = {(0,2), (1,0), (5,1)}
```

Candidate ID c58472; canonical key
`bb2c3fe1d7472b16b19489dd`; proposal kernel
`validated-highd-two-step`; parent `validated-c45508-648-4-36`. The final
all-rung document and adjacent full verdict are under
`research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`. The
collision-safe review copies are `research/candidates/648-8-30-c58472.json`
and `research/candidates/648-8-30-c58472.verdict.json`.
