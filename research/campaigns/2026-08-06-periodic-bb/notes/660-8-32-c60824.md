# [[660,8,32]] — distinct epoch-6 BB mutation c60824

## Direction & hypothesis

Apply a correlated support mutation to the trusted `[[660,8,32]]` c54828
parent on `Z_30 x Z_11`, preserving `k=8` while probing the local
high-distance basin. Candidate c60824 is connected and advances weight-6 ×
unrestricted. Its distance is a witness-backed upper bound; literature
novelty is unverified.

## What was searched

Epoch 6 covered IDs 60663--63662: 3,000 deterministic proposals, 552 new
encoders, and 523 connected records. Eighteen connected children were frozen
for the distance-first screen. c60824 uses `validated-highd-correlated`,
canonical key `88b5fb7e84ef5bd868f9c2a5`, and proposal seed
`12366320632712291689`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(58,44)` at 400,
`(38,36)` at 8k, `(34,36)` at 60k, and `(32,36)` at 1M. All-rung
consolidation retained the ladder `44→36→34→32`, supporting `d<=32`, not an
exact distance. Trusted validation passed with seed `527409498` and marked
the code board-advancing. `k^2*d/n=3.10`; board FOM `k*d^2/n=12.41`. This is
a fourth structurally distinct campaign support at `[[660,8,32]]`, still
locally dominated by the campaign's `[[660,8,34]]` points. Final artifact:
`c0060824-88b5fb7e84-epoch6-primary-final-allrungs-s2141703080.json`.

## Dead ends

The shallow estimate was inflated by 12 and continued descending at every
deeper rung. The final support ties c51753, c51899, and c54828 rather than
raising the local `Z_30 x Z_11` distance ceiling.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=1`, four threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_11` with `A=[(2,3),(8,1),(10,8)]` and
`B=[(3,1),(6,3),(7,5)]`; candidate c60824 uses
`validated-highd-correlated` with proposal seed `12366320632712291689`.
