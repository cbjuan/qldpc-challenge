# [[686,12,30]] — reserve high-distance BB mutation c57634

## Direction & hypothesis

Use a two-step mutation of the trusted `[[686,6,36]]` parent on
`Z_49 x Z_7`, seeking to retain much of its distance while doubling the
encoded dimension. Candidate c57634 is connected and advances weight-6 ×
unrestricted. Its distance is a witness-backed upper bound; literature
novelty is unverified.

## What was searched

Epoch 4 covered IDs 54663--57662: 3,000 deterministic proposals, 369 new
encoders, and 345 connected records. A disjoint 12-code reserve queue kept
additional connected children of the highest-distance parents. c57634 uses
`validated-highd-two-step`, canonical key `4a0a53c8649a293d7b857c61`, and
proposal seed `13507004426636592639`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(48,36)` at 400,
`(34,40)` at 8k, `(34,36)` at 60k, and `(30,36)` at 1M. All-rung
consolidation retained the ladder `36→34→34→30`, supporting `d<=30`, not an
exact distance. Trusted validation passed with seed `1586667414` and marked
the code board-advancing. `k^2*d/n=6.30`; board FOM `k*d^2/n=15.74`. The
support is distinct but locally dominated by the smaller `[[576,12,30]]`.
Final artifact:
`c0057634-4a0a53c864-epoch4-reserve-final-allrungs-s2014784105.json`.

## Dead ends

The estimate appeared stable at 34 through 60k, but the 1M rung found the
decisive weight-30 X logical. The result does not improve the local parameter
frontier after c56213, yet confirms that the `Z_49 x Z_7` d=36 parent can
produce connected `k=12,d=30` children.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_49 x Z_7` with `A=[(2,4),(8,2),(11,0)]` and
`B=[(2,6),(6,3),(7,4)]`; candidate c57634 uses
`validated-highd-two-step` with proposal seed `13507004426636592639`.
