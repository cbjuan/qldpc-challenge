# [[504,8,28]] — exact `Z_12 x Z_21` transfer of the known d=18 BB support

## Direction & hypothesis

Sweep exact rectangular-torus transfers of the known `[[288,12,18]]`
bivariate-bicycle support, looking for larger weight-6 codes whose distance
continues to grow along the extended axis.

## What was searched

Candidate c45094 was screened inside the bounded 18-code parent-`d>=14`
exact-geometry batch.  Its Python-400 document was saved before promotion to
fresh Python/accelerated 8k and reused-Python accelerated 60k and 1M rungs.
All witnesses and raw accelerated evidence were persisted.  This code is
connected.

## Evidence trail

The persisted `(X,Z)` witness weights were:

- Python 400: `(42,36)`;
- Python 8k and accelerated 8k: `(30,36)`;
- accelerated 60k: `(28,36)`;
- accelerated 1M: `(28,36)` using seed 311597738;
- all-rung consolidation: `(28,36)`.

The submission honestly claims the witness-backed upper bound `d <= 28`;
there is no exact-distance proof.  The final document embeds explicit X-28
and Z-36 logicals.  Trusted validation passed with seed 1431976649, found no
lighter logical in 8,000 RIS trials, found no exact or WL-equivalent board
entry, and labels the code as advancing weight-6 x unrestricted.  Literature
novelty is unverified.

- Campaign FOM: `k^2*d/n = 3.5556`.
- Board FOM: `k*d^2/n = 12.4444`.
- Canonical artifact: `c0045094-8e0e297932-grid-final-allrungs-s584792725.json`.

## Dead ends

Python 400 overestimated the minimum witness at 36; the X side fell first to
30 and then to 28.  The 1M rung did not lower it further.  The same grid also
contained c45110, which collapsed to `[[504,16,4]]` and was not promoted.

## Tools

Codex agent harness; `build_bb`, deterministic campaign selection and
packaging, `gf2_fast`, trusted Python GF(2) witness checks, and
`validate_candidate`.  The full ladder used 400 and fresh 8k Python trials,
then 8k + 60k + 1M accelerated trials.

## Reproduction

On `Z_12 x Z_21`, use:

```text
A = {(0,2), (0,7), (3,0)}
B = {(0,3), (1,0), (2,0)}
HX = [A | B], HZ = [B^T | A^T]
```

Candidate ID c45094; canonical key
`8e0e297932d3cb31a52b3544`.  The canonical all-rung document and verdict are
under `research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`.
