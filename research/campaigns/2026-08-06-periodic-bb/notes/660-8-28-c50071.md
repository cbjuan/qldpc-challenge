# [[660,8,28]] — distinct high-distance BB mutation c50071

## Direction & hypothesis

Apply a second one-support mutation near the trusted `[[660,8,34]]` parent
on `Z_30 x Z_11`, seeking another connected `k=8` high-distance support.
Candidate c50071 advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound; literature novelty is unverified.

## What was searched

The second validated-parent epoch covered 2,000 deterministic proposals at
IDs 49163--51162; its distance-first queue retained 12 connected candidates.
c50071 uses `validated-highd-local-one`, canonical key
`cf4e7464e29f5408f6320f95`, and proposal seed `6392045218324846254`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(32,32)` at 400 and
`(28,28)` at each of 8k, 60k, and 1M. All-rung consolidation retained the
ladder `32→28→28→28`, supporting `d<=28`, not an exact distance. Trusted
validation passed with seed `958340511` and marked the code board-advancing.
`k^2*d/n=2.72`; board FOM `k*d^2/n=9.50`. The support is structurally
distinct but locally dominated by `[[660,8,34]]`. Final artifact:
`c0050071-cf4e7464e2-validated-highd-epoch2-nonroot-final-allrungs-s1210538772.json`.

## Dead ends

The distance stabilized after 8k, but the resulting parameter point is below
the campaign's local `Z_30 x Z_11` frontier. It remains useful mutation data.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_11` with `A=[(2,2),(8,1),(10,6)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c50071 uses
`validated-highd-local-one` with proposal seed `6392045218324846254`.
