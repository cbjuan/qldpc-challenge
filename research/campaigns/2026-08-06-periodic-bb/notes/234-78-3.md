# [[234,78,3]] — one-third-rate weight-6 periodic BB

## Direction & hypothesis

Search the unrestricted weight-6 high-rate gap at `145 <= n <= 287` using
arbitrary unequal BB supports. Shared binomial factors can raise k sharply;
the question was whether any connected example would retain a witnessed
distance above the unhelpful `d=2` plateau.

## What was searched

This candidate came from the first 20,000-proposal deterministic structural
census and its `shared-binomial-factor` lane. It was one of 40 records in the
saved promotion batch and was screened at 400 Python witness trials (seed
1447009269) before deeper confirmation.

## Evidence trail

The lightest witnessed logical stayed at 3 through the full saved ladder:
400 Python trials, 8,000 Python plus 8,000 accelerated trials (accelerated
seed 1877024465), and 8,000 Python plus 60,000 accelerated trials. The final
accelerated weight-3 X witness at seed 196597832 passed the trusted Python
GF(2) checks. Validation at seed 1600650354 passed, found no lighter logical
in its independent 8,000-trial search, found no board duplicate, and labeled
the code as advancing weight-6 x unrestricted.

Claim: witness-backed `d <= 3`, not exact. Literature novelty is unverified.

FOMs: `k^2*d/n = 78^2*3/234 = 78.00`;
`k*d^2/n = 78*9/234 = 3.00`.

## Dead ends

The same shared-factor route also produced `[[224,112,2]]`: more rate, but
only a weight-2 logical. Thus the factor mechanism still limits distance;
this `d=3` survivor clears the campaign's useful-distance floor but has only
`k*d^2/n = 3.00`.

## Tools

Local autoresearch agent harness, periodic BB constructor and submission kit,
`gf2_fast` at pair depth 16, trusted Python GF(2) witness checks, and the local
trusted validator. The approximate per-candidate confirmation work was two
8,000-trial Python passes plus 8,000 and 60,000 accelerated passes. All
artifacts remain local and unsubmitted.

## Reproduction

On `Z_9 x Z_13`, use
`A={(0,0),(6,0)}` and `B={(0,0),(2,2),(5,2),(6,0)}`.
Candidate ID 6423; the 400/8k/60k candidates, raw accelerated evidence, and
full validator verdict are under `../artifacts/candidates/`.
