# [[660,8,34]] — distinct high-distance BB mutation c48101

## Direction & hypothesis

Apply a local support step on `Z_30 x Z_11`, seeking a connected weight-6 BB
code that preserves the strong `[[660,8,34]]` parameter point with a distinct
support. Candidate c48101 advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound, and literature novelty is unverified.

## What was searched

The high-distance continuation lane screened local and correlated mutations
around calibrated `d>=14` geometries. c48101 uses
`high-distance-local-step`, canonical key `90fdf16c76dcff01e74121ab`, and
proposal seed `3851572456202034343`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(52,54)` at Python 400, `(48,38)` at
accelerated 8k, `(38,38)` at 60k, and `(34,38)` at 1M. Final all-rung
consolidation retained `dX<=34` and `dZ<=38`, giving the upper-bound ladder
`52→38→38→34` over 400→8k→60k→1M. This supports `d<=34`, not an exact
distance. Trusted validation passed with seed `1791004509`, found no exact or
WL-equivalent board entry, and marked the candidate board-advancing.
`k^2*d/n=3.30`; board FOM `k*d^2/n=14.01`. Artifact:
`c0048101-90fdf16c76-continuation-final-allrungs-s1427629452.json`.

## Dead ends

The shallow estimate fell from 52 to 38 at 8k and to 34 at 1M. Seven
continuation finalists passed the trusted gate, but four were nonadvancing;
only the three advancing results were checkpointed.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_30 x Z_11` with `A=[(2,4),(8,2),(10,8)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c48101 uses
`high-distance-local-step`.
