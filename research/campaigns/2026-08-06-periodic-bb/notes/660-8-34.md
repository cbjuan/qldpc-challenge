# [[660,8,34]] — high-distance local BB mutation on Z_30 x Z_11

## Direction & hypothesis

Perturb a sparse high-distance BB parent while preserving useful logical
dimension and the campaign's weight-6 checks. Candidate c46841 advances
weight-6 × unrestricted. Its distance is witness-backed upper-bound evidence,
and literature novelty is unverified.

## What was searched

The `parent14` high-distance-transfer lane explored local and correlated
support mutations. c46841 uses `high-distance-local-step` and canonical key
`1fc0bc7d325c77832846a9f6`.

## Evidence trail

The upper-bound ladder tightened `38→36→34→34` at
400→8k→60k→1M. The final consolidated document records `dX<=34` and
`dZ<=40`. Trusted validation passed with seed `867376987`, reporting board
advancement. `k^2*d/n=3.30`; board FOM `14.01`. Artifact:
`c0046841-1fc0bc7d32-parent14-final-allrungs-s1631360436.json`.

## Dead ends

Nineteen parent14 finalists passed the trusted gate; 11 were nonadvancing and
remain unstaged audit results. This candidate survived the full evidence
ladder.

## Tools

Campaign persistence tooling, research-kit packaging, `gf2_fast`
(`pair_depth=16`, four threads), all-rung consolidation, and trusted validation.

## Reproduction

Use `Z_30 x Z_11`, `A=[(2,4),(8,1),(10,6)]`, and
`B=[(3,0),(6,3),(7,5)]`; rebuild c46841 with
`high-distance-local-step`.
