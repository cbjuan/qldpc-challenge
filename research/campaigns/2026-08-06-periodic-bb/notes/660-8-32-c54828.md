# [[660,8,32]] — distinct epoch-4 BB mutation c54828

## Direction & hypothesis

Apply a paired local-support mutation to a trusted `[[660,8,34]]` parent on
`Z_30 x Z_11`, seeking another connected support near the local distance
ceiling. Candidate c54828 advances weight-6 × unrestricted. Its distance is
a witness-backed upper bound; literature novelty is unverified.

## What was searched

Epoch 4 covered IDs 54663--57662: 3,000 deterministic proposals, 369 new
encoders, and 345 connected records. Eighteen connected children of
`parent_d>=28` were frozen for the distance-first screen. c54828 uses
`validated-highd-local-pair`, canonical key `09ff68dae866969e4e8ac704`, and
proposal seed `9385998849590891677`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(52,64)` at 400,
`(40,34)` at 8k and 60k, and `(32,34)` at 1M. All-rung consolidation
retained the ladder `52→34→34→32`, supporting `d<=32`, not an exact
distance. Trusted validation passed with seed `1253756839` and marked the
code board-advancing. `k^2*d/n=3.10`; board FOM `k*d^2/n=12.41`. This is a
third structurally distinct local support at `[[660,8,32]]`, still dominated
by the campaign's `[[660,8,34]]` points. Final artifact:
`c0054828-09ff68dae8-epoch4-c54828-final-allrungs-s1270196823.json`.

## Dead ends

The shallow estimate was inflated by 20, and the decisive weight-32 X
logical appeared only at 1M after the candidate had held at 34 through 60k.
It did not raise the local `Z_30 x Z_11` distance ceiling.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_11` with `A=[(2,3),(8,2),(10,8)]` and
`B=[(3,2),(6,3),(7,5)]`; candidate c54828 uses
`validated-highd-local-pair` with proposal seed `9385998849590891677`.
