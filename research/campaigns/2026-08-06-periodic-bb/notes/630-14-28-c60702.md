# [[630,14,28]] — major high-k/high-distance BB sweet spot

## Direction & hypothesis

Search the weight-6 × unrestricted cell inside the user-locked periodic
bivariate-bicycle family, prioritizing witnessed distance at least 8 and then
useful encoded dimension. Candidate c60702 is a connected two-step mutation
of the locally validated `[[630,8,34]]` c50584 support on the same `15 × 21`
torus. The mutation raised structural `k` from 8 to 14 while testing how much
of the parent's high-distance behavior survives.

## What was searched

Epoch 6 deterministically censused 3,300 proposals, IDs c60663–c63962: 589
encoded and 558 connected. Eighteen connected records were frozen in the
globally distance-prior/child-`k`-optimal
`selection-refreshed-highd-epoch6-001.json` under caps of two per parent, four
per geometry, and six per mutation kernel. c60702 was a parent-`d=34`,
child-`k=14` selection with proposal seed `3987466261156539550`, then screened
at Python 400, fresh Python 8k, and accelerated 8k, 60k, and 1M rungs.

## Evidence trail

The persisted side-aware witness ladder was:

- Python 400, seed `1513160`: `(X,Z)=(32,36)`, hence `d<=32`;
- Python 8k, seed `2087547754`: `(30,28)`, hence `d<=28`;
- accelerated 8k, seed `2087547771`: raw Z-32, retaining `(30,28)`;
- accelerated 60k, seed `171065496`: X-28, giving `(28,28)`;
- accelerated 1M, seed `593269361`: X-28, retaining `(28,28)`;
- all-rung consolidation, seed `740444642`: `(28,28)` from seven persisted
  submission documents.

The final claim is the witness-backed upper bound `d <= 28`, not an exact
distance. The consolidated artifact embeds independently checked explicit X
and Z logicals, both of weight 28. Trusted validation passed with seed
`238282746`, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent public-board entry, and labels the code as advancing the
weight-6 × unrestricted board. Literature novelty is unverified.

This is a major new local useful-`k`/high-distance sweet spot. Relative to its
`[[630,8,34]]` parent it exchanges six units of witnessed distance for six
additional logical qubits, and it improves the prior local `[[630,14,20]]`
point by eight distance units at the same `n` and `k`.

- Campaign FOM: `k^2*d/n = 8.7111`.
- Board FOM: `k*d^2/n = 17.4222`.
- Canonical artifact:
  `c0060702-44aec63f56-epoch6-final-allrungs-s740444642.json`.

## Dead ends

The 400-trial estimate of 32 collapsed to 28 at fresh Python 8k. The
accelerated 8k search found only a heavier Z-32 logical; 60k then balanced the
sides at 28, and 1M did not reduce them further. The child therefore did not
inherit parent distance 34, but its large gain in `k` survived the full
confirmation ladder.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first
`campaign.py` runner, `research/kit/submit.py`, `gf2_fast`, trusted Python
witness checks, and `verify/validate_candidate.py`. The ladder used one
400-trial screen, one fresh Python-8k search, accelerated 8k + 60k + 1M at
pair depth 16 on eight threads, and the trusted 8k-RIS gate.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_15 × Z_21`:

```text
A = {(0,4), (0,20), (3,0)}
B = {(0,6), (7,0), (14,18)}
```

Candidate ID c60702; canonical key
`44aec63f56b3847377d8810c`; proposal kernel
`validated-highd-two-step`; parent `validated-c50584-630-8-34`. The final
all-rung document and adjacent full verdict are under
`research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`. The
collision-safe review copies are `research/candidates/630-14-28-c60702.json`
and `research/candidates/630-14-28-c60702.verdict.json`.
