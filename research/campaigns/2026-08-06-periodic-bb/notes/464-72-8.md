# [[464,72,8]] — connected high-rate BB transfer on Z_29 x Z_8

## Direction & hypothesis

Transfer the campaign's high-rate `d=8` support to a nearby rectangular torus,
seeking more encoded qubits while retaining the high-distance promotion floor.
Candidate c45030 is connected and advances weight-8 × unrestricted. The
distance claim is an upper bound, and literature novelty is unverified.

## What was searched

The high-distance exact-geometry grid varied torus dimensions around the local
`[[272,48,8]]` support while keeping its eight monomials fixed. c45030 uses
proposal kernel `exact-geometry-grid` and canonical key
`7ccd7f7b7e704ed6a4fbba41`.

## Evidence trail

Every persisted rung retained `(dX,dZ)=(8,8)`: Python 400, fresh Python and
accelerated 8k, accelerated 60k, accelerated 1M, and final all-rung
consolidation. The canonical document embeds explicit weight-8 X and Z
logicals, so the claim is `d<=8` with upper-bound confidence, not an exact
distance. Trusted validation passed with seed `363290906` and marked the code
board-advancing. `k^2*d/n=89.38`; board FOM `k*d^2/n=9.93`. Canonical artifact:
`c0045030-7ccd7f7b7e-1m-python8000reuse8k-s88208784-fast1000000-allrungs.json`.

## Dead ends

A later audit-all-saved consolidation reproduced this canonical candidate and
did not supply a new parameter point or lighter witness. It is retained as
audit evidence, not counted as a second find.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_29 x Z_8` with
`A=[(0,0),(0,5),(1,0),(1,5)]` and
`B=[(0,0),(0,1),(5,0),(5,1)]`; candidate c45030 uses
`exact-geometry-grid`.
