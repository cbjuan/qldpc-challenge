# [[540,8,28]] — distinct high-distance BB mutation c50670

## Direction & hypothesis

Apply a one-support mutation to the trusted `[[540,8,30]]` parent on
`Z_30 x Z_9`, retaining `k=8` near the local distance ceiling. Candidate
c50670 is connected and advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound; literature novelty is unverified.

## What was searched

The second validated-parent epoch covered 2,000 deterministic proposals at
IDs 49163--51162; its distance-first queue retained 12 connected candidates.
c50670 uses `validated-highd-local-one`, canonical key
`a15b7d30032ec6afae7c32f4`, and proposal seed `5426662695505530151`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(42,40)` at 400,
`(34,30)` at 8k, `(32,30)` at 60k, and `(28,30)` at 1M. All-rung
consolidation retained the ladder `40→30→30→28`, supporting `d<=28`, not an
exact distance. Trusted validation passed with seed `348535414` and marked
the code board-advancing. `k^2*d/n=3.32`; board FOM `k*d^2/n=11.61`. The
support is structurally distinct but locally dominated by the campaign's
`[[540,8,30]]` points. Final artifact:
`c0050670-a15b7d3003-validated-highd-epoch2-nonroot-final-allrungs-s1506171802.json`.

## Dead ends

The decisive X logical appeared only at 1M, lowering the estimate from 30 to
28. The candidate advances the public board but does not improve the local
`Z_30 x Z_9` parameter frontier.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_9` with `A=[(2,4),(8,2),(10,8)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c50670 uses
`validated-highd-local-one` with proposal seed `5426662695505530151`.
