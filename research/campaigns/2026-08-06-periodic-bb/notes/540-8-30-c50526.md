# [[540,8,30]] — third distinct high-distance BB support c50526

## Direction & hypothesis

Apply a two-step support mutation to the validated `[[540,8,30]]` parent,
seeking another connected high-distance code with useful `k=8`. Candidate
c50526 is the third structurally distinct campaign support at these
parameters and advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound, and literature novelty is unverified.

## What was searched

The second validated-high-distance mutation epoch sampled trusted
`d=12--36` parents with several support-preserving kernels. c50526 is a
two-step child of c46282 with canonical key `935d73d62a3fb22e41ae08f2` and
proposal seed `13137514732959063269`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(36,44)` at Python 400, `(36,36)` at
accelerated 8k, `(36,30)` at 60k, and `(30,30)` at 1M. Final all-rung
consolidation retained `dX<=30` and `dZ<=30`, giving the upper-bound ladder
`36→36→30→30` over 400→8k→60k→1M. This supports `d<=30`, not an exact
distance. Trusted validation passed with seed `2081365120`, found no exact or
WL-equivalent board entry, and marked the candidate board-advancing.
`k^2*d/n=3.56`; board FOM `k*d^2/n=13.33`. Artifact:
`c0050526-935d73d62a-epoch2-002-final-allrungs-s225017734.json`.

## Dead ends

The 400 and 8k values agreed at 36, but the 60k rung found a weight-30
Z-logical and the 1M rung found a matching X-logical. This is another direct
example of why apparent 8k flatness cannot replace deep self-refutation.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_30 x Z_9` with `A=[(2,4),(8,2),(9,8)]` and
`B=[(3,0),(6,3),(7,8)]`; candidate c50526 uses
`validated-highd-two-step`.
