# [[660,8,28]] — correlated high-distance BB mutation c50719

## Direction & hypothesis

Use a correlated support mutation near the trusted `[[660,8,34]]` parent on
`Z_30 x Z_11`, preserving `k=8` while exploring a different local direction.
Candidate c50719 advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound; literature novelty is unverified.

## What was searched

The second validated-parent epoch covered 2,000 deterministic proposals at
IDs 49163--51162; its distance-first queue retained 12 connected candidates.
c50719 uses `validated-highd-correlated`, canonical key
`973d53c0d78147d28beb62dc`, and proposal seed `6474765296262003627`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(48,54)` at 400,
`(40,32)` at 8k and 60k, and `(28,32)` at 1M. All-rung consolidation
retained the ladder `48→32→32→28`, supporting `d<=28`, not an exact
distance. Trusted validation passed with seed `2136966436` and marked the
code board-advancing. `k^2*d/n=2.72`; board FOM `k*d^2/n=9.50`. The support
is structurally distinct but locally dominated by `[[660,8,34]]`. Final
artifact: `c0050719-973d53c0d7-validated-highd-epoch2-nonroot-final-allrungs-s2076383251.json`.

## Dead ends

This was the strongest shallow inflater in the batch: the estimate fell by
20, with the decisive weight-28 X logical appearing only at 1M.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_11` with `A=[(2,4),(8,2),(10,6)]` and
`B=[(3,0),(6,3),(7,6)]`; candidate c50719 uses
`validated-highd-correlated` with proposal seed `6474765296262003627`.
