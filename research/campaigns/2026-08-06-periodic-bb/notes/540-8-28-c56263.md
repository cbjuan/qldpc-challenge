# [[540,8,28]] — distinct epoch-4 BB mutation c56263

## Direction & hypothesis

Apply a paired local mutation to the trusted `[[540,8,30]]` parent on
`Z_30 x Z_9`, preserving `k=8` while probing nearby high-distance supports.
Candidate c56263 is connected and advances weight-6 × unrestricted. Its
distance is a witness-backed upper bound; literature novelty is unverified.

## What was searched

Epoch 4 covered IDs 54663--57662: 3,000 deterministic proposals, 369 new
encoders, and 345 connected records. Eighteen connected children of
`parent_d>=28` were frozen for the distance-first screen. c56263 uses
`validated-highd-local-pair`, canonical key
`b7526a2ad501a3dafa477376`, and proposal seed `17386851833384346134`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(38,36)` at 400 and
`(28,28)` at 8k, 60k, and 1M. All-rung consolidation retained the ladder
`36→28→28→28`, supporting `d<=28`, not an exact distance. Trusted
validation passed with seed `804878326` and marked the code board-advancing.
`k^2*d/n=3.32`; board FOM `k*d^2/n=11.61`. The support is structurally
distinct but locally dominated by the campaign's `[[540,8,30]]` points.
Final artifact:
`c0056263-b7526a2ad5-epoch4-primary-final-allrungs-s1073690573.json`.

## Dead ends

The shallow `d=36` signal was strongly inflated: both logical sides fell to
28 at 8k and stayed there through 1M. The mutation therefore did not improve
the trusted `[[540,8,30]]` parent locally.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_9` with `A=[(2,4),(8,2),(9,0)]` and
`B=[(3,0),(6,3),(7,3)]`; candidate c56263 uses
`validated-highd-local-pair` with proposal seed `17386851833384346134`.
