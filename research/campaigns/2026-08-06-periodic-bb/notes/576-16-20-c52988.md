# [[576,16,20]] — distinct refreshed high-distance BB mutation c52988

## Direction & hypothesis

Use the refreshed validated-parent epoch to seek a connected code that keeps
the useful `k=16, d=20` balance while exploring a support distinct from
c50641. Candidate c52988 advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound, and literature novelty is unverified.

## What was searched

The refreshed epoch sampled connected mutations of 15 gate-passing parents,
with priority on parents witnessed at `d>=28`. c52988 is a two-step child of
c45091 with canonical key `c5f0902d470bca9c8ecc1151` and proposal seed
`5465795564561681844`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(24,20)` at Python 400 and `(20,20)`
at accelerated 8k, 60k, and 1M. Final all-rung consolidation retained
`dX<=20` and `dZ<=20`, giving the flat upper-bound ladder `20→20→20→20` over
400→8k→60k→1M. This supports `d<=20`, not an exact distance. Trusted
validation passed with seed `1832190016`, found no exact or WL-equivalent
board entry, and marked the candidate board-advancing. `k^2*d/n=8.89`;
board FOM `k*d^2/n=11.11`. Artifact:
`c0052988-c5f0902d47-epoch3-final-allrungs-s2067371932.json`.

## Dead ends

The child did not improve the local parameter point established by c50641,
but it reached the same useful balance through a distinct support and stayed
flat through the full deep ladder.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_12 x Z_24` with `A=[(0,2),(0,7),(3,21)]` and
`B=[(0,3),(1,0),(2,0)]`; candidate c52988 uses
`validated-highd-two-step` in the refreshed epoch.
