# [[540,8,26]] — distinct refreshed BB mutation c51763

## Direction & hypothesis

Mutate the trusted `Z_30 x Z_9` high-distance parent while retaining `k=8`.
Candidate c51763 is connected and advances weight-6 × unrestricted. Its
distance is a witness-backed upper bound; literature novelty is unverified.

## What was searched

The refreshed 3,000-proposal epoch yielded 508 encoders and 476 connected
records across all 15 parents and five kernels. c51763 uses
`validated-highd-local-pair`, canonical key `b9db84dcd544a9387aa049bc`,
and proposal seed `15384460907713894517`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(32,36)` at Python 400, `(30,26)` at
8k, `(30,26)` at 60k, and `(26,26)` at 1M. All-rung consolidation retained
the ladder `32→26→26→26`, supporting `d<=26`, not an exact distance. Trusted
validation passed with seed `1849932528` and marked the code board-advancing.
`k^2*d/n=3.08`; board FOM `k*d^2/n=10.01`. The point ties the distinct local
c49482 `[[540,8,26]]` support and is locally dominated by the campaign's
unsubmitted `[[540,8,30]]` supports. Final artifact:
`c0051763-b9db84dcd5-epoch3-final-allrungs-s1256549432.json`.

## Dead ends

The 8k rung found the decisive Z logical at weight 26. The same epoch's
highest-k structural child, c52130, collapsed to `[[630,20,4]]`; c51768
retained `k=16` but only `d=8`.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_9` with `A=[(2,4),(8,2),(9,6)]` and
`B=[(3,0),(6,3),(7,6)]`; candidate c51763 uses
`validated-highd-local-pair` with proposal seed `15384460907713894517`.
