# [[686,18,24]] — high-k/high-distance BB sweet spot

## Direction & hypothesis

Search the weight-6 × unrestricted cell inside the user-locked periodic
bivariate-bicycle family, prioritizing witnessed distance at least 8 while
retaining useful encoded dimension. Candidate c58579 is a connected two-step
mutation of the locally validated `[[686,12,30]]` c57634 support on the same
`49 × 7` torus. The mutation traded some parent distance for six additional
logical qubits, targeting a better high-`d`, high-`k` balance.

## What was searched

Epoch 5 deterministically censused 3,000 proposals, IDs c57663–c60662. It
produced 440 encoders, of which 419 were connected. A disjoint seven-record
high-`k` balance queue was frozen in
`selection-refreshed-highk-balance-epoch5-001.json`. c58579 was the queue's
unique `k=18` record, with parent-distance prior 30 and proposal seed
`8777816445396654416`. It was screened at Python 400, fresh Python 8k, and
accelerated 8k, 60k, and 1M rungs.

## Evidence trail

The persisted side-aware witness ladder was:

- Python 400, seed `1783898887`: `(X,Z)=(32,24)`, hence `d<=24`;
- Python 8k, seed `1521741804`: `(24,24)`;
- accelerated 8k, seed `1521741821`: X-24, retaining `(24,24)`;
- accelerated 60k, seed `1956017887`: X-24, retaining `(24,24)`;
- accelerated 1M, seed `1590003357`: X-24, retaining `(24,24)`;
- all-rung consolidation, seed `146694265`: `(24,24)` from seven persisted
  submission documents.

The scalar distance ladder was flat at 24 from the first screen through 1M.
The final claim is the witness-backed upper bound `d <= 24`, not an exact
distance. The consolidated artifact embeds independently checked explicit X
and Z logicals, both of weight 24. Trusted validation passed with seed
`1383179841`, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent public-board entry, and labels the code as advancing the
weight-6 × unrestricted board. Literature novelty is unverified.

This is a new local high-`k`/high-distance sweet spot rather than a domination
of its `[[686,12,30]]` parent: it exchanges six units of witnessed distance
for six additional logical qubits.

- Campaign FOM: `k^2*d/n = 11.3353`.
- Board FOM: `k*d^2/n = 15.1137`.
- Canonical artifact:
  `c0058579-4962667b1a-epoch5-balance-final-allrungs-s146694265.json`.

## Dead ends

The shallow scalar result was already 24 because Python 400 found the Z-24
logical, although its X side was inflated at 32. Fresh Python 8k reduced X to
24, and none of the accelerated rungs improved either side. The mutation did
not inherit parent distance 30, but its `k=18` tradeoff survived the full
confirmation ladder.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first
`campaign.py` runner, `research/kit/submit.py`, `gf2_fast`, trusted Python
witness checks, and `verify/validate_candidate.py`. Per code the ladder used
one 400-trial screen, one fresh Python-8k search, accelerated 8k + 60k + 1M
searches at pair depth 1 on four threads, and the trusted 8k-RIS gate.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_49 × Z_7`:

```text
A = {(1,3), (9,2), (11,0)}
B = {(2,6), (6,3), (7,4)}
```

Candidate ID c58579; canonical key
`4962667b1a9b5e01f92905bc`; proposal kernel
`validated-highd-two-step`; parent `validated-c57634-686-12-30`. The final
all-rung document and adjacent full verdict are under
`research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`. The
collision-safe review copies are `research/candidates/686-18-24-c58579.json`
and `research/candidates/686-18-24-c58579.verdict.json`.
