# [[660,8,32]] — refreshed high-distance BB mutation c51753

## Direction & hypothesis

Mutate a trusted `[[660,8,34]]` parent on `Z_30 x Z_11` while preserving
useful `k=8` and high distance. Candidate c51753 is connected and advances
weight-6 × unrestricted. Its distance is a witness-backed upper bound;
literature novelty is unverified.

## What was searched

The refreshed epoch covered IDs 51663--54662: 3,000 deterministic proposals,
exactly 40 per parent/kernel pair, yielding 508 encoders and 476 connected
records. Eighteen candidates received 400-trial witnesses. c51753 uses
`validated-highd-local-pair`, canonical key
`557ee969d1792089c483f09f`, and proposal seed `5131093002914780247`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(52,50)` at Python 400, `(42,34)` at
8k, `(38,34)` at 60k, and `(38,32)` at 1M. All-rung consolidation retained
the ladder `50→34→34→32`, supporting `d<=32`, not an exact distance. Trusted
validation passed with seed `1838837001` and marked the code board-advancing.
`k^2*d/n=3.10`; board FOM `k*d^2/n=12.41`. The parameter point ties the
distinct c51899 support and is locally dominated by the campaign's unsubmitted
`[[660,8,34]]` supports. Final artifact:
`c0051753-557ee969d1-epoch3-final-allrungs-s707541541.json`.

## Dead ends

In the same shallow batch, c52130 fell to `[[630,20,4]]`; c51768 fell to
`[[600,16,8]]`; and c51799, c54229, and c52220 reached only `d=12`.
c52181 survived to `[[630,12,22]]` but passed as board-non-advancing.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_11` with `A=[(2,4),(8,2),(10,7)]` and
`B=[(3,0),(6,3),(7,3)]`; candidate c51753 uses
`validated-highd-local-pair` with proposal seed `5131093002914780247`.
