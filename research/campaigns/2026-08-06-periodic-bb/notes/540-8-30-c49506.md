# [[540,8,30]] — distinct two-step high-distance BB mutation c49506

## Direction & hypothesis

Use a two-step support mutation of the validated `[[540,8,30]]` parent to seek
a structurally distinct code at the same strong parameter point. Candidate
c49506 is connected and advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound, and literature novelty is unverified.

## What was searched

The second validated-high-distance mutation epoch sampled trusted
`d=12--36` parents with several support-preserving kernels. c49506 is a
two-step child of c46282 with canonical key `a6d5cd81d03302341571756f`
and proposal seed `238207271814703187`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(38,40)` at Python 400, `(32,34)` at
accelerated 8k, `(32,32)` at 60k, and `(30,32)` at 1M. Final all-rung
consolidation retained `dX<=30` and `dZ<=32`, so the upper-bound ladder was
`38→32→32→30` over 400→8k→60k→1M. This supports `d<=30`, not an exact
distance. Trusted validation passed with seed `1423771336`, found no exact or
WL-equivalent board code, and marked the candidate board-advancing.
`k^2*d/n=3.56`; board FOM `k*d^2/n=13.33`. Artifact:
`c0049506-a6d5cd81d0-epoch2-highd-final-allrungs-s1497264527.json`.

## Dead ends

The shallow estimate fell from 38 to 32 at 8k and finally to 30 at 1M. The
existing c46282 shares these parameters, so the c49506 suffix is retained to
avoid hiding that this is a distinct support rather than a new parameter point.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_30 x Z_9` with `A=[(2,2),(8,1),(9,8)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c49506 uses
`validated-highd-two-step`.
