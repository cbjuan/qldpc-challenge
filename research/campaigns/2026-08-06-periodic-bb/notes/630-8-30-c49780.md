# [[630,8,30]] — distinct local-pair high-distance BB mutation c49780

## Direction & hypothesis

Apply a paired local support mutation to the validated `[[630,8,30]]`
parent, seeking another connected high-distance code with useful `k=8`.
Candidate c49780 is structurally distinct from the campaign's existing
support at these parameters and advances weight-6 × unrestricted. Its
distance is a witness-backed upper bound, and literature novelty is
unverified.

## What was searched

The second validated-high-distance mutation epoch sampled trusted
`d=12--36` parents with several support-preserving kernels. c49780 is a
local-pair child of c45118 with canonical key `9658f1b20b40d6615fb5e766` and
proposal seed `9171047916149594597`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(44,40)` at Python 400, `(32,32)` at
accelerated 8k, `(32,32)` at 60k, and `(30,32)` at 1M. Final all-rung
consolidation retained `dX<=30` and `dZ<=32`, giving the upper-bound ladder
`40→32→32→30` over 400→8k→60k→1M. This supports `d<=30`, not an exact
distance. Trusted validation passed with seed `1510748770`, found no exact or
WL-equivalent board entry, and marked the candidate board-advancing.
`k^2*d/n=3.05`; board FOM `k*d^2/n=11.43`. Artifact:
`c0049780-9658f1b20b-epoch2-001-final-allrungs-s292983485.json`.

## Dead ends

The code tied the existing local `[[630,8,30]]` point rather than improving
it. Its 8k and 60k values of 32 fell to 30 only at 1M, again demonstrating why
the large-code packaging floor is necessary.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_15 x Z_21` with `A=[(0,2),(2,4),(3,0)]` and
`B=[(0,6),(7,0),(13,0)]`; candidate c49780 uses
`validated-highd-local-pair`.
