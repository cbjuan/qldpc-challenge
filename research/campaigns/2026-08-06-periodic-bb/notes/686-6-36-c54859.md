# [[686,6,36]] — distinct high-distance BB mutation c54859

## Direction & hypothesis

Mutate the trusted `[[686,6,36]]` support on `Z_49 x Z_7` while preserving
its useful logical dimension and high witnessed distance. Candidate c54859 is
connected and advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound; literature novelty is unverified.

## What was searched

The refreshed epoch-4 census covered 3,000 deterministic proposals at IDs
54663--57662. A disjoint 12-record reserve retained useful-`k` children of
the strongest high-distance parents. c54859 uses
`validated-highd-two-step`, canonical key
`dc48c8eeefff97c2050b8e54`, and proposal seed
`11817573187436291818`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(40,42)` at 400,
`(38,40)` at 8k, and `(36,40)` at both 60k and 1M. All-rung consolidation
retained the ladder `40→38→36→36`, supporting `d<=36`, not an exact
distance. Trusted validation passed with refutation seed `1892109994` and
marked the code board-advancing. `k^2*d/n=1.89`; board FOM
`k*d^2/n=11.34`. Final artifact:
`c0054859-dc48c8eeef-epoch4-reserve-final-allrungs-s1063744359.json`.

## Dead ends

The shallow `d=40` estimate did not survive the confirmation ladder: the 8k
and 60k rungs found lighter X logicals. The final result ties the parameters
of c48487 rather than raising the local distance ceiling, but uses a distinct
support and independently advances the public board.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, Python 400/8k witness searches, `gf2_fast` (`pair_depth=1`, four
threads) at 8k/60k/1M, all-rung witness consolidation, and the pinned trusted
validator.

## Reproduction

Build on `Z_49 x Z_7` with `A=[(2,4),(7,3),(11,0)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c54859 uses
`validated-highd-two-step` with proposal seed `11817573187436291818`.
