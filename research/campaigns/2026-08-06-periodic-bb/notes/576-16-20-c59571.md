# [[576,16,20]] — structurally distinct BB tie

## Direction & hypothesis

Search the weight-6 × unrestricted cell inside the user-locked periodic
bivariate-bicycle family, prioritizing witnessed distance at least 8 while
retaining useful encoded dimension. Candidate c59571 is a connected two-step
mutation of the locally validated `[[576,16,20]]` c52988 support on the same
`12 × 24` torus. The goal was to test whether the useful `k=16`, `d=20`
parameter point persists under a structurally different support.

## What was searched

Epoch 5 deterministically censused 3,000 proposals, IDs c57663–c60662. It
produced 440 encoders, of which 419 were connected. A disjoint seven-record
high-`k` balance queue was frozen in
`selection-refreshed-highk-balance-epoch5-001.json`. c59571 was its unique
`k=16` record, with parent-distance prior 20 and proposal seed
`15618423332025845495`. It was screened at Python 400, fresh Python 8k, and
accelerated 8k, 60k, and 1M rungs.

## Evidence trail

The persisted side-aware witness ladder was:

- Python 400, seed `1225701855`: `(X,Z)=(22,20)`, hence `d<=20`;
- Python 8k, seed `598840365`: `(20,20)`;
- accelerated 8k, seed `598840382`: X-20, retaining `(20,20)`;
- accelerated 60k, seed `1083468968`: X-20, retaining `(20,20)`;
- accelerated 1M, seed `1374841713`: X-20, retaining `(20,20)`;
- all-rung consolidation, seed `785985312`: `(20,20)` from seven persisted
  submission documents.

The scalar distance ladder was flat at 20 from the first screen through 1M.
The final claim is the witness-backed upper bound `d <= 20`, not an exact
distance. The consolidated artifact embeds independently checked explicit X
and Z logicals, both of weight 20. Trusted validation passed with seed
`1477498938`, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent public-board entry, and labels the code as advancing the
weight-6 × unrestricted board. Literature novelty is unverified.

The support is structurally distinct but ties the two existing local
`[[576,16,20]]` c50641 and c52988 results; it adds reproducibility and support
diversity rather than a new numerical Pareto point.

- Campaign FOM: `k^2*d/n = 8.8889`.
- Board FOM: `k*d^2/n = 11.1111`.
- Canonical artifact:
  `c0059571-185be85cbe-epoch5-balance-final-allrungs-s785985312.json`.

## Dead ends

The shallow scalar result was already 20 because Python 400 found the Z-20
logical, while its X side was 22. Fresh Python 8k reduced X to 20, and none of
the accelerated rungs found a lighter witness. The mutation therefore tied
its parent instead of improving the local parameters.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first
`campaign.py` runner, `research/kit/submit.py`, `gf2_fast`, trusted Python
witness checks, and `verify/validate_candidate.py`. Per code the ladder used
one 400-trial screen, one fresh Python-8k search, accelerated 8k + 60k + 1M
searches at pair depth 1 on four threads, and the trusted 8k-RIS gate.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_12 × Z_24`:

```text
A = {(0,2), (0,7), (3,21)}
B = {(0,3), (2,1), (2,23)}
```

Candidate ID c59571; canonical key
`185be85cbee676d9acee5a90`; proposal kernel
`validated-highd-two-step`; parent `validated-c52988-576-16-20`. The final
all-rung document and adjacent full verdict are under
`research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`. The
collision-safe review copies are `research/candidates/576-16-20-c59571.json`
and `research/candidates/576-16-20-c59571.verdict.json`.
