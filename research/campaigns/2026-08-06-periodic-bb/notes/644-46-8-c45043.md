# [[644,46,8]] c45043 — structurally distinct exact-support BB on Z_23 x Z_14

## Direction & hypothesis

Extend the local `[[270,18,9]]` supports across an exact-geometry grid while
enforcing `d>=8`. The parameter point was already reached locally by c46163,
but c45043 is a distinct construction (`bd31490bf3...` versus
`2e3e5a3d1a...`). It advances weight-6 × unrestricted; literature novelty is
unverified.

## What was searched

Selection 016 screened 32 connected exact-grid records at Python-400.
Candidate c45043 has full canonical key `bd31490bf37075eeec2ed5c5` and was one
of 28 records promoted through the deep ladder.

## Evidence trail

The witnessed distance remained `8` through 400→8k→60k→1M. All-rung
consolidation selected weight-8 X and Z logicals. The trusted gate passed with
seed `1599592777` and marked the candidate board-advancing. `k^2*d/n=26.29`;
board FOM `4.57`. Artifact:
`c0045043-bd31490bf3-1m-python8000reuse8k-s128654370-fast1000000-allrungs.json`.

## Dead ends

Four shallow records missed the floor (c45024=7, c45029=6, c45034=5,
c45038=7). No promoted record collapsed. This code adds a structure at an
existing local parameter point rather than a net-new parameter triple.

## Tools

Persistence-safe `campaign.py`, research-kit packaging, `gf2_fast`
(`pair_depth=16`, four threads), all-rung witness consolidation, and trusted
validation.

## Reproduction

Build on `Z_23 x Z_14` with `A=[(0,0),(0,11),(7,2),(7,8)]` and
`B=[(0,0),(0,1)]`; use c45043 from
`selection-highd-exact-rate-001-survivors.json`.
