# [[576,24,12]] — two-component exact transfer of the known d=14 BB support

## Direction & hypothesis

Transfer the known `[[360,16,14]]` periodic bivariate-bicycle support to
larger rectangular tori while preserving its six monomials.  The `Z_24 x
Z_12` geometry increases encoded dimension and tests whether the parent's
distance scale persists.

## What was searched

Candidate c45107 was one of 18 parent-`d>=14` exact-geometry grid points
screened at Python 400 and then promoted through fresh 8k, 60k, and 1M.  Its
interaction graph has two equal 288-qubit components; the trusted verifier
accepts the resulting CSS code and recomputes `[[576,24,12]]`.

## Evidence trail

Every persisted rung found the same `(X,Z)=(12,12)` witness weights:

- Python 400;
- fresh Python 8k and accelerated 8k;
- accelerated 60k;
- accelerated 1M using seed 1711831873;
- final all-rung consolidation.

The claim is `d <= 12` with upper-bound confidence, not an exact distance.
The final document embeds explicit weight-12 X and Z logicals.  Trusted
validation passed with seed 1508261927, found no lighter logical in 8,000 RIS
trials, found no exact or WL-equivalent board entry, and labels the code as
advancing weight-6 x unrestricted.  Literature novelty is unverified.

- Campaign FOM: `k^2*d/n = 12.0000`.
- Board FOM: `k*d^2/n = 6.0000`.
- Canonical artifact: `c0045107-6cf5e50841-grid-final-allrungs-s953228287.json`.

## Dead ends

No distance collapse appeared through 1M, but the construction decomposes
into two equal components and must not be described as a new connected code.
Another exact-grid point, c45110, collapsed to `[[504,16,4]]` at the shallow
rung.

## Tools

Codex agent harness; periodic-BB constructor, component-aware structural
ledger, persistence-first submission path, `gf2_fast`, trusted Python witness
checks, and the trusted validator.  The ladder used Python 400 and fresh
Python 8k plus 8k, 60k, and 1M accelerated searches.

## Reproduction

Build on `Z_24 x Z_12` with:

```text
A = {(0,2), (0,4), (3,0)}
B = {(0,6), (7,0), (14,0)}
HX = [A | B], HZ = [B^T | A^T]
```

Candidate ID c45107; canonical key
`6cf5e50841833a4e88988d4d`.  The canonical all-rung document and verdict are
under `research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`.
