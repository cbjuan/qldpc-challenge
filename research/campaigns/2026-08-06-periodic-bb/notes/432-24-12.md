# [[432,24,12]] — compact two-component transfer of the known d=14 BB support

## Direction & hypothesis

Use exact geometry transfers of the known `[[360,16,14]]` bivariate-bicycle
support to seek denser encoded-dimension points that remain above the d=8
promotion floor.  `Z_18 x Z_12` is the smallest selected long-axis extension
in this branch.

## What was searched

Candidate c45113 was included in the bounded 18-point parent-`d>=14` grid.
The campaign saved its Python-400 witness and every fresh-8k, 60k, and 1M
document and evidence file.  Structurally, the code has two equal 216-qubit
interaction components.

## Evidence trail

The persisted ladder was flat at `(X,Z)=(12,12)`:

- Python 400;
- fresh Python and accelerated 8k;
- accelerated 60k;
- accelerated 1M using seed 892217895;
- final all-rung consolidation.

This certifies only the upper bound `d <= 12`; it is not an exact-distance
claim.  Explicit weight-12 X and Z witnesses are embedded in the canonical
artifact.  Trusted validation passed with seed 2032750440, found no lighter
logical in 8,000 RIS trials, found no exact or WL-equivalent board entry, and
labels the code as advancing weight-6 x unrestricted.  Literature novelty is
unverified.

- Campaign FOM: `k^2*d/n = 16.0000`.
- Board FOM: `k*d^2/n = 8.0000`.
- Canonical artifact: `c0045113-03423dcc22-grid-final-allrungs-s444909809.json`.

## Dead ends

The witness did not collapse through 1M, but the two-component decomposition
is a material structural limitation and is recorded rather than hidden.
Grid candidate c45110 collapsed to distance 4 and did not enter the deep
ladder.

## Tools

Codex agent harness; `research/kit/bb.py`, structural component profiling,
the persistence-first campaign and submission path, `gf2_fast`, trusted
Python witness checks, and `validate_candidate`.  Compute comprised Python
400 and fresh Python 8k plus accelerated 8k, 60k, and 1M rungs.

## Reproduction

On `Z_18 x Z_12`, build:

```text
A = {(0,2), (0,4), (3,0)}
B = {(0,6), (7,0), (14,0)}
HX = [A | B], HZ = [B^T | A^T]
```

Candidate ID c45113; canonical key
`03423dcc2208e2f868062233`.  The canonical all-rung document and verdict are
under `research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`.
