# [[700,50,8]] — exact-support BB continuation on Z_25 x Z_14

## Direction & hypothesis

Extend the local `[[270,18,9]]` support pattern over an exact rectangular grid,
keeping the hard `d>=8` floor while gaining logical dimension. The result
advances weight-6 × unrestricted; literature novelty is unverified.

## What was searched

Selection 016 screened 32 connected exact-geometry records at Python-400.
Candidate c45041 has canonical key `d2d733f70e1e777cab3d26ee` and was one of 28
records sent through 8k, 60k, and 1M.

## Evidence trail

The witnessed distance remained `8` at 400→8k→60k→1M. All-rung consolidation
kept weight-8 X and Z logicals, and the trusted gate passed with seed
`251888676`, marking the point board-advancing. `k^2*d/n=28.57`; board FOM
`4.57`. Artifact:
`c0045041-d2d733f70e-1m-python8000reuse8k-s1358071328-fast1000000-allrungs.json`.

## Dead ends

Four of the 32 Python-400 records missed the floor: c45024 (`7`), c45029 (`6`),
c45034 (`5`), and c45038 (`7`). None of the 28 promoted records collapsed.

## Tools

`campaign.py`, kit packaging, `gf2_fast` (`pair_depth=16`, four threads), and
the trusted validator.

## Reproduction

Build on `Z_25 x Z_14` with `A=[(0,0),(0,11),(7,2),(7,8)]` and
`B=[(0,0),(0,1)]`; use candidate c45041 from the survivor selection.
