# [[648,8,32]] — two-step mutation of the local d=34 BB support

## Direction & hypothesis

Search the weight-6 × unrestricted cell inside the user-locked periodic
bivariate-bicycle family, prioritizing witnessed distance at least 8 and then
useful encoded dimension. Candidate c58638 is a connected two-step mutation
of the locally validated `[[648,8,34]]` c45088 support on the same `12 × 27`
torus. The parent's distance was used only as a scheduling prior; the child
had to produce and preserve its own witnesses.

## What was searched

Epoch 5 deterministically censused 3,000 proposals, IDs c57663–c60662. It
produced 440 encoders, of which 419 were connected. Eighteen connected
records were frozen in `selection-refreshed-highd-epoch5-001.json` under caps
of two per parent, four per geometry, and six per mutation kernel. c58638 was
selected from the parent-`d=34` layer with proposal seed
`15899052779619224437`, then screened at Python 400, fresh Python 8k, and
accelerated 8k, 60k, and 1M rungs.

## Evidence trail

The persisted side-aware witness ladder was:

- Python 400, seed `1616243653`: `(X,Z)=(54,58)`, hence `d<=54`;
- Python 8k, seed `224102533`: `(42,46)`, hence `d<=42`;
- accelerated 8k, seed `224102550`: X-42, retaining `(42,46)`;
- accelerated 60k, seed `2126747612`: Z-40, giving `(42,40)`;
- accelerated 1M, seed `1457212558`: X-32, giving `(32,40)`;
- all-rung consolidation, seed `183048936`: `(32,40)` from seven persisted
  submission documents.

The final claim is the witness-backed upper bound `d <= 32`, not an exact
distance. The consolidated artifact embeds independently checked explicit X
and Z logicals of weights 32 and 40. Trusted validation passed with seed
`1599688728`, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent public-board entry, and labels the code as advancing the
weight-6 × unrestricted board. Literature novelty is unverified.

Locally, this point is dominated by the campaign's unsubmitted
`[[648,8,34]]` c45088 result with the same `n` and `k`; public-board advancement
and local Pareto status are therefore different statements.

- Campaign FOM: `k^2*d/n = 3.1605`.
- Board FOM: `k*d^2/n = 12.6420`.
- Canonical artifact:
  `c0058638-324eb6a044-epoch5-final-allrungs-s183048936.json`.

## Dead ends

The 400-trial estimate of 54 collapsed to 42 at 8k. Although the 60k rung
still showed 40 and cleared the deepening threshold, the 1M rung found the
weight-32 X logical. The child therefore did not retain its parent's local
distance-34 point, demonstrating again that parent distance and shallow
distance are ranking priors rather than inherited evidence.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first
`campaign.py` runner, `research/kit/submit.py`, `gf2_fast`, trusted Python
witness checks, and `verify/validate_candidate.py`. The accelerated searches
used pair depth 16 and eight threads. Deep witness work plus validation took
about seven minutes of wall time for this code, excluding the structural
census.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_12 × Z_27`:

```text
A = {(0,2), (0,7), (3,0)}
B = {(0,1), (1,2), (2,0)}
```

Candidate ID c58638; canonical key
`324eb6a044dc8b8048fbb37d`; proposal kernel
`validated-highd-two-step`; parent `validated-c45088-648-8-34`. The final
all-rung document and adjacent full verdict are under
`research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`. The
collision-safe review copies are `research/candidates/648-8-32-c58638.json`
and `research/candidates/648-8-32-c58638.verdict.json`.
