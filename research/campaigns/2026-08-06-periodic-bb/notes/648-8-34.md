# [[648,8,34]] — long-axis exact transfer of the known d=18 BB support

## Direction & hypothesis

Extend the known `[[288,12,18]]` periodic bivariate-bicycle support while
holding its `l=12` axis fixed.  The exact-support transfer to `Z_12 x Z_27`
tests whether the parent's high-distance geometry persists on a longer torus
without changing weight 6.

## What was searched

Candidate c45088 was one of 18 already-encoded, parent-`d>=14`
exact-geometry grid points in `selection-highd-exact-grid-001.json`.  All were
packaged at 400 Python trials; 17 shallow survivors were promoted through a
fresh Python/accelerated 8k rung, an accelerated 60k rung, and an accelerated
1M rung.  The 60k and 1M runs reused the persisted Python-8k base.  This code
is connected.

## Evidence trail

The persisted `(X,Z)` witness weights were:

- Python 400: `(46,48)`;
- Python 8k: `(44,42)`;
- accelerated 8k: `(34,42)`;
- accelerated 60k: `(36,42)` raw, with the lighter saved X-34 witness retained;
- accelerated 1M: `(34,42)` using seed 463713366;
- all-rung consolidation: `(34,42)`.

The final claim is the witness-backed upper bound `d <= 34`, not an exact
distance.  The consolidated artifact embeds independently checked explicit X
and Z logicals of weights 34 and 42.  Trusted validation passed with seed
1354105119, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent board entry, and labels the code as advancing the weight-6 x
unrestricted board.  Literature novelty is unverified.

- Campaign FOM: `k^2*d/n = 3.3580`.
- Board FOM: `k*d^2/n = 14.2716`.
- Canonical artifact: `c0045088-c6719414b0-grid-final-allrungs-s454801597.json`.

## Dead ends

The 400-trial estimate of 46 was inflated and fell to 34 at 8k.  In the same
18-code grid screen, c45110 collapsed to `[[504,16,4]]`; parent distance and
an exact support transfer were therefore ranking priors, not inherited
evidence.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first campaign
runner, `research/kit/submit.py`, `gf2_fast`, trusted Python witness checks,
and `verify/validate_candidate.py`.  Per promoted code the ladder used one
400-trial screen, one fresh Python-8k search, and 8k + 60k + 1M accelerated
trials.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_12 x Z_27`:

```text
A = {(0,2), (0,7), (3,0)}
B = {(0,3), (1,0), (2,0)}
```

Candidate ID c45088; canonical key
`c6719414b059bb6e27d4d1b3`.  The canonical all-rung document and verdict are
under `research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`.
