# [[630,14,28]] — structurally distinct high-k/high-distance BB tie

## Direction & hypothesis

Search the weight-6 × unrestricted cell inside the user-locked periodic
bivariate-bicycle family, prioritizing witnessed distance at least 8 and then
useful encoded dimension. Candidate c62627 is a connected two-step mutation
of the locally validated `[[630,8,34]]` c50584 support on the same `15 × 21`
torus. The mutation raised structural `k` from 8 to 14 while testing how much
of the parent's high-distance behavior survives.

## What was searched

Epoch 6 deterministically censused 3,300 proposals, IDs c60663–c63962: 589
encoded and 558 connected. A disjoint seven-record high-`k` balance queue was
frozen in `selection-refreshed-highk-balance-epoch6-001.json`. c62627 was its
parent-`d=34`, child-`k=14` selection with proposal seed
`1165624485201814551`, then screened at Python 400, fresh Python 8k, and
accelerated 8k, 60k, and 1M rungs.

## Evidence trail

The persisted side-aware witness ladder was:

- Python 400, seed `1837001285`: `(X,Z)=(44,40)`, hence `d<=40`;
- Python 8k, seed `955958706`: `(32,36)`, hence `d<=32`;
- accelerated 8k, seed `955958723`: Z-30, giving `(32,30)` and `d<=30`;
- accelerated 60k, seed `626503925`: X-28, giving `(28,30)` and `d<=28`;
- accelerated 1M, seed `1063820819`: X-28, retaining `(28,30)`;
- all-rung consolidation, seed `1919330075`: `(28,30)` from seven persisted
  submission documents.

The final claim is the witness-backed upper bound `d <= 28`, not an exact
distance. The consolidated artifact embeds independently checked explicit X
and Z logicals of weights 28 and 30. Trusted validation passed with seed
`239110215`, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent public-board entry, and labels the code as advancing the
weight-6 × unrestricted board. Literature novelty is unverified.

This support is structurally distinct but ties the earlier local
`[[630,14,28]]` c60702 result. It therefore adds reproducibility and support
diversity at a strong high-`k`/high-distance point rather than a new numerical
Pareto point.

- Campaign FOM: `k^2*d/n = 8.7111`.
- Board FOM: `k*d^2/n = 17.4222`.
- Canonical artifact:
  `c0062627-d9b124ae1a-epoch6-balance-final-allrungs-s1919330075.json`.

## Dead ends

The 400-trial estimate of 40 fell to 32 in fresh Python 8k and to 30 in the
accelerated 8k search. The 60k rung then found the final X-28 witness; 1M did
not reduce either side further. The mutation did not preserve the parent's
witnessed distance 34, but its six-qubit gain in `k` survived the full
confirmation ladder.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first
`campaign.py` runner, `research/kit/submit.py`, `gf2_fast`, trusted Python
witness checks, and `verify/validate_candidate.py`. The ladder used one
400-trial screen, one fresh Python-8k search, accelerated 8k + 60k + 1M at
pair depth 1 on four threads, and the trusted 8k-RIS gate.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_15 × Z_21`:

```text
A = {(0,2), (0,6), (3,1)}
B = {(0,6), (7,0), (14,18)}
```

Candidate ID c62627; canonical key
`d9b124ae1ab1e08a38d68a5a`; proposal kernel
`validated-highd-two-step`; parent `validated-c50584-630-8-34`. The final
all-rung document and adjacent full verdict are under
`research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`. The
collision-safe review copies are `research/candidates/630-14-28-c62627.json`
and `research/candidates/630-14-28-c62627.verdict.json`.
