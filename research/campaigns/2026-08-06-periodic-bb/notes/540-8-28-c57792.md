# [[540,8,28]] — distinct epoch-5 BB mutation c57792

## Direction & hypothesis

Apply a paired local mutation to the trusted `[[540,8,30]]` parent on
`Z_30 x Z_9`, preserving `k=8` while probing its high-distance neighborhood.
Candidate c57792 is connected and advances weight-6 × unrestricted. Its
distance is a witness-backed upper bound; literature novelty is unverified.

## What was searched

Epoch 5 covered IDs 57663--60662: 3,000 deterministic proposals, 440 new
encoders, and 419 connected records. Eighteen connected children were frozen
for the distance-first screen. c57792 uses `validated-highd-local-pair`,
canonical key `39166808d3e4a60e0fa46c4e`, and proposal seed
`2059778339774455005`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(40,36)` at 400,
`(34,36)` at 8k, `(34,30)` at 60k, and `(28,30)` at 1M. All-rung
consolidation retained the ladder `36→34→30→28`, supporting `d<=28`, not an
exact distance. Trusted validation passed with seed `1267882800` and marked
the code board-advancing. `k^2*d/n=3.32`; board FOM `k*d^2/n=11.61`. The
support is structurally distinct but locally dominated by the campaign's
`[[540,8,30]]` points. Final artifact:
`c0057792-39166808d3-epoch5-primary-final-allrungs-s1328386581.json`.

## Dead ends

The shallow `d=36` signal fell at every deeper stage: X dropped to 34 at 8k,
Z dropped to 30 at 60k, and the decisive X=28 logical appeared only at 1M.
The candidate advances the public board but does not improve the local
`Z_30 x Z_9` parameter frontier.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=1`, four threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_9` with `A=[(2,4),(8,2),(10,8)]` and
`B=[(3,0),(6,4),(7,5)]`; candidate c57792 uses
`validated-highd-local-pair` with proposal seed `2059778339774455005`.
