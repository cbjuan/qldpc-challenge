# [[448,70,8]] — high-rate exact-support BB continuation on Z_28 x Z_8

## Direction & hypothesis

Extend the validated local `[[272,48,8]]` support pattern along an exact-geometry
grid while enforcing the campaign's hard `d>=8` floor and prioritizing
`k^2*d/n`. This candidate advances weight-8 × unrestricted. Literature novelty
is unverified.

## What was searched

Selection `selection-016.json` screened 32 connected exact-geometry records at
Python-400. Candidate c45031 (canonical key `06114122222c6c16ae48b1bb`)
survived into the 28-record confirmation set.

## Evidence trail

The witnessed distance stayed flat at `8` through 400→8k→60k→1M trials.
The final all-rung document consolidates Python-8k and fast 8k/60k/1M evidence;
both X and Z witnesses have weight 8. The trusted gate passed with seed
`698966490` and marked the code board-advancing. `k^2*d/n=87.50`; board FOM
`k*d^2/n=10.00`. Artifact:
`c0045031-0611412222-1m-python8000reuse8k-s2058039681-fast1000000-allrungs.json`.

## Dead ends

The same 32-record screen rejected c45024 (`d=7`), c45029 (`d=6`), c45034
(`d=5`), and c45038 (`d=7`) at Python-400. None of the 28 promoted records
collapsed at 8k, 60k, or 1M.

## Tools

`campaign.py`, the research kit submission path, and `gf2_fast` with
`pair_depth=16`, four threads; trusted validation used the pinned verifier.

## Reproduction

Build the periodic BB code on `Z_28 x Z_8` with
`A=[(0,0),(0,5),(1,0),(1,5)]` and
`B=[(0,0),(0,1),(5,0),(5,1)]`; candidate c45031 is recorded in
`selection-highd-exact-rate-001-survivors.json`.
