# [[660,8,32]] — distinct refreshed high-distance BB mutation c51899

## Direction & hypothesis

Apply a one-support mutation to a trusted `[[660,8,34]]` parent on
`Z_30 x Z_11`, seeking a second connected high-distance `k=8` structure.
Candidate c51899 advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound; literature novelty is unverified.

## What was searched

The refreshed epoch covered IDs 51663--54662: 3,000 deterministic proposals,
508 encoders, and 476 connected records. Eighteen candidates received the
400-trial screen. c51899 uses `validated-highd-local-one`, canonical key
`30e29b574c424445afc607c1`, and proposal seed `10870027528471915858`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(42,40)` at Python 400, `(34,36)` at
8k, `(34,32)` at 60k, and `(32,32)` at 1M. All-rung consolidation retained
the ladder `40→34→32→32`, supporting `d<=32`, not an exact distance. Trusted
validation passed with seed `1923019792` and marked the code board-advancing.
`k^2*d/n=3.10`; board FOM `k*d^2/n=12.41`. This is structurally distinct
from c51753 at the same parameters, and both are locally dominated by the
campaign's unsubmitted `[[660,8,34]]` supports. Final artifact:
`c0051899-30e29b574c-epoch3-final-allrungs-s76572769.json`.

## Dead ends

The shallow `d=40` estimate fell to 34 at 8k and 32 at 60k. Five candidates
in the 18-code screen fell below the `d>=16` promotion floor, while three
additional `d=20--26` candidates were preserved but excluded by the initial
promotion cap.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_30 x Z_11` with `A=[(2,4),(8,1),(10,6)]` and
`B=[(3,1),(6,3),(7,5)]`; candidate c51899 uses
`validated-highd-local-one` with proposal seed `10870027528471915858`.
