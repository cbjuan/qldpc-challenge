# [[630,10,28]] — useful-k mutation of a d=34 BB support

## Direction & hypothesis

Search the weight-6 × unrestricted cell inside the user-locked periodic
bivariate-bicycle family, prioritizing witnessed distance at least 8 and then
useful encoded dimension. Candidate c60611 is a connected two-step mutation
of the locally validated `[[630,8,34]]` c50584 support on the same `15 × 21`
torus. The mutation raised structural `k` from 8 to 10; the parent's distance
was only a scheduling prior and was not attributed to the child.

## What was searched

Epoch 5 deterministically censused 3,000 proposals, IDs c57663–c60662. It
produced 440 encoders, of which 419 were connected. Eighteen connected
records were frozen in `selection-refreshed-highd-epoch5-001.json` under caps
of two per parent, four per geometry, and six per mutation kernel. c60611 was
selected from the parent-`d=34` layer with proposal seed
`6485964398477110535`, then screened at Python 400, fresh Python 8k, and
accelerated 8k, 60k, and 1M rungs.

## Evidence trail

The persisted side-aware witness ladder was:

- Python 400, seed `192567329`: `(X,Z)=(40,46)`, hence `d<=40`;
- Python 8k, seed `1066055489`: `(28,32)`, hence `d<=28`;
- accelerated 8k, seed `1066055506`: X-30, retaining `(28,32)`;
- accelerated 60k, seed `2059443277`: X-28, retaining `(28,32)`;
- accelerated 1M, seed `1522388817`: X-28, retaining `(28,32)`;
- all-rung consolidation, seed `1316218620`: `(28,32)` from seven persisted
  submission documents.

The final claim is the witness-backed upper bound `d <= 28`, not an exact
distance. The consolidated artifact embeds independently checked explicit X
and Z logicals of weights 28 and 32. Trusted validation passed with seed
`1027386003`, found no lighter logical in 8,000 RIS trials, found no exact or
WL-equivalent public-board entry, and labels the code as advancing the
weight-6 × unrestricted board. Literature novelty is unverified.

Locally, the campaign's unsubmitted `[[576,12,30]]` result has smaller `n`
and larger `k` and `d`, so this public-board advance is not on the accumulated
local Pareto frontier.

- Campaign FOM: `k^2*d/n = 4.4444`.
- Board FOM: `k*d^2/n = 12.4444`.
- Canonical artifact:
  `c0060611-ecad34533e-epoch5-final-allrungs-s1316218620.json`.

## Dead ends

The 400-trial estimate of 40 collapsed to 28 at the fresh Python-8k rung.
Neither accelerated 8k, 60k, nor 1M found a lighter logical, but the child did
not retain its parent's distance-34 point. The result again separates a useful
parent-distance prior from evidence about a mutated child.

## Tools

Codex agent harness; `research/kit/bb.py`, the persistence-first
`campaign.py` runner, `research/kit/submit.py`, `gf2_fast`, trusted Python
witness checks, and `verify/validate_candidate.py`. Per code the ladder used
one 400-trial screen, one fresh Python-8k search, accelerated 8k + 60k + 1M
searches at pair depth 1 on four threads, and the trusted 8k-RIS gate.

## Reproduction

Build `HX=[A|B]` and `HZ=[B^T|A^T]` with `build_bb` on `Z_15 × Z_21`:

```text
A = {(0,2), (0,4), (5,19)}
B = {(0,6), (7,0), (14,18)}
```

Candidate ID c60611; canonical key
`ecad34533e34338633c5439e`; proposal kernel
`validated-highd-two-step`; parent `validated-c50584-630-8-34`. The final
all-rung document and adjacent full verdict are under
`research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`. The
collision-safe review copies are `research/candidates/630-10-28-c60611.json`
and `research/candidates/630-10-28-c60611.verdict.json`.
