# [[648,8,30]] — distinct two-step high-distance BB mutation c49503

## Direction & hypothesis

Use a two-step support mutation of the validated `[[648,8,34]]` parent to
seek another connected high-distance code with useful `k=8`. Candidate c49503
advances the public weight-6 × unrestricted board. It is locally dominated by
the campaign's `[[648,8,34]]` supports, its distance is a witness-backed upper
bound, and literature novelty is unverified.

## What was searched

The second validated-high-distance mutation epoch sampled trusted
`d=12--36` parents with several support-preserving kernels. c49503 is a
two-step child of c45088 with canonical key `03d691baccf9ff9eca8612a4` and
proposal seed `8758874546659980302`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(50,54)` at Python 400, `(34,36)` at
accelerated 8k, `(32,36)` at 60k, and `(30,36)` at 1M. Final all-rung
consolidation retained `dX<=30` and `dZ<=36`, giving the upper-bound ladder
`50→34→32→30` over 400→8k→60k→1M. This supports `d<=30`, not an exact
distance. Trusted validation passed with seed `691473579`, found no exact or
WL-equivalent board entry, and marked the candidate board-advancing.
`k^2*d/n=2.96`; board FOM `k*d^2/n=11.11`. Artifact:
`c0049503-03d691bacc-epoch2-highd-final-allrungs-allrungs-s240151815.json`.

## Dead ends

The shallow estimate fell from 50 to 34 at 8k, then to 32 at 60k and 30 at
1M. The honest result remains useful but is locally dominated by c45088 and
other campaign supports at `[[648,8,34]]`; it is retained because the trusted
gate independently marked this distinct support as advancing the public board.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_12 x Z_27` with `A=[(0,2),(0,7),(3,0)]` and
`B=[(0,1),(1,0),(2,2)]`; candidate c49503 uses
`validated-highd-two-step`.
