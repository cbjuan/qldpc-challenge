# [[648,4,36]] — high-distance local BB mutation on Z_12 x Z_27

## Direction & hypothesis

Make a local support mutation near a high-distance BB parent, trading logical
rate for a stronger witnessed distance. c45508 advances weight-6 ×
unrestricted. The distance remains an upper bound; literature novelty is
unverified.

## What was searched

The parent14 high-distance-transfer lane explored local and correlated support
steps. c45508 has canonical key `b6632bcfdadfa365e8f8b7c8` and proposal kernel
`high-distance-local-step`.

## Evidence trail

The witness sequence tightened `54→44→38→36` over
400→8k→60k→1M. The final consolidated document records `dX<=36` and
`dZ<=48`. The trusted gate passed with seed `838681361` and labeled it
board-advancing. `k^2*d/n=0.89`; board FOM `8.00`. Artifact:
`c0045508-b6632bcfda-parent14-final-allrungs-s981475032.json`.

## Dead ends

Eleven of 19 trusted parent14 finalists did not advance the board. They remain
as audit data; only the eight advancing candidates were staged.

## Tools

Persistence-first campaign tools, `gf2_fast` with pair depth 16 and four
threads, final all-rung consolidation, and the pinned validator.

## Reproduction

Build on `Z_12 x Z_27` with `A=[(0,2),(0,7),(3,0)]` and
`B=[(0,3),(1,0),(4,2)]`; candidate c45508 uses
`high-distance-local-step`.
