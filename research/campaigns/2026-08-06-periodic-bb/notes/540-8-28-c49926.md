# [[540,8,28]] — distinct two-step high-distance BB mutation c49926

## Direction & hypothesis

Apply a two-step support mutation to the validated `[[540,8,30]]` parent,
seeking another connected high-distance code with useful `k=8`. Candidate
c49926 is structurally distinct from the campaign's existing `[[540,8,28]]`
support and advances weight-6 × unrestricted. Its distance is a
witness-backed upper bound, and literature novelty is unverified.

## What was searched

The second validated-high-distance mutation epoch sampled trusted
`d=12--36` parents with several support-preserving kernels. c49926 is a
two-step child of c46282 with canonical key `af02e440d68f12f0447df34e` and
proposal seed `2767723112777865711`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(38,32)` at Python 400 and `(28,28)`
at each of accelerated 8k, 60k, and 1M. Final all-rung consolidation retained
`dX<=28` and `dZ<=28`, giving the upper-bound ladder `32→28→28→28` over
400→8k→60k→1M. This supports `d<=28`, not an exact distance. Trusted
validation passed with seed `792254865`, found no exact or WL-equivalent board
entry, and marked the candidate board-advancing. `k^2*d/n=3.32`; board FOM
`k*d^2/n=11.61`. Artifact:
`c0049926-af02e440d6-epoch2-001-final-allrungs-s1255640216.json`.

## Dead ends

The code does not improve the campaign's local parameter frontier because
c47122 already has `[[540,8,28]]`. It is retained as a separately validated
support and stayed flat after the first deep 8k rung.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, four threads), all-rung witness
consolidation, trusted Python witness checks, and the pinned validator.

## Reproduction

Build on `Z_30 x Z_9` with `A=[(1,5),(7,1),(9,8)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c49926 uses
`validated-highd-two-step`.
