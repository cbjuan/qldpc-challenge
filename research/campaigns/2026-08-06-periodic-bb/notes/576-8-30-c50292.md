# [[576,8,30]] — distance-first BB mutation c50292

## Direction & hypothesis

Probe connected mutations of trusted periodic-BB parents for a useful balance
between high distance and retained encoded dimension. Candidate c50292 is a
weight-6 code on `Z_24 x Z_12` with `k=8`. Its distance is a witness-backed
upper bound; literature novelty is unverified.

## What was searched

The second validated-parent epoch structurally censused 2,000 deterministic
proposals at IDs 49163--51162. Its first promotion file contained 32 connected
candidates, 30 of which remained in the survivor queue. c50292 uses the
`validated-highd-correlated` kernel, canonical key
`ed8ba0775e224119d4d46238`, and proposal seed `13389960678472776146`.

## Evidence trail

The persisted `(dX,dZ)` witnesses were `(44,46)` at Python 400, `(40,36)` at
8k, `(38,32)` at 60k, and `(30,32)` at 1M. All-rung consolidation retained
the ladder `44→36→32→30`, supporting `d<=30`, not an exact distance. Trusted
validation passed with seed `1172652839` and marked the code board-advancing.
`k^2*d/n=3.33`; board FOM `k*d^2/n=12.50`. Final artifact:
`c0050292-ed8ba0775e-epoch2-c50292-deep-1m-20260807-allrungs-s1532178643.json`.

## Dead ends

The shallow estimate inflated by 14 over the full ladder, including drops at
every deeper allocation: `44→36→32→30`. This candidate would have been
materially overstated without the 1M rung and all-rung witness merge.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=16`, eight threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_24 x Z_12` with `A=[(0,4),(1,3),(3,0)]` and
`B=[(0,6),(7,0),(15,1)]`; candidate c50292 uses
`validated-highd-correlated` with proposal seed `13389960678472776146`.
