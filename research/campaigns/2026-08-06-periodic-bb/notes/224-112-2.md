# [[224,112,2]] — half-rate weight-6 periodic BB

## Direction & hypothesis

High-rate gap in the unrestricted weight-6 cell, using arbitrary periodic BB
supports rather than the previously mined 3+3 ansatz.

## What was searched

First 20,000-proposal structural census; shared-binomial-factor proposal,
selected as a calibration representative because its exact structural value
`k=112` cleared the automatic high-rate threshold.

## Evidence trail

The persisted 2,000-trial search found `d <= 2`. Trusted validation passed,
found no weight-1 logical in 8,000 RIS trials at seed 2053987649, and labeled it
as advancing the weight-6 × unrestricted board. Upper bound only; literature
novelty unverified.

## Dead ends

The construction confirms that the most extreme common-factor rates pay for
their rate with very low distance. Future selection down-weights this kernel.

## Tools

Periodic `bb.py`, persistence-first campaign runner, and trusted validator.

## Reproduction

Build on `Z_8 x Z_14` with
`A={(0,0),(3,9),(4,7),(7,2)}` and `B={(0,0),(4,7)}`.
Full candidate and verdict: candidate ID 12783 under `../artifacts/candidates/`.
