# [[630,8,30]] — exact `Z_15 x Z_21` transfer of the known d=14 BB support

## Direction & hypothesis

Extend the known `[[360,16,14]]` periodic bivariate-bicycle support along its
second torus axis, trading rate for a larger witnessed distance while keeping
weight 6 and the support itself fixed.

## What was searched

Candidate c45118 came from the 18-code parent-`d>=14` exact-geometry screen.
After its saved Python-400 result, it entered a fresh Python/accelerated 8k
rung and reused that Python base for accelerated 60k and 1M searches.  Every
witness and raw evidence file was persisted.  This code is connected.

## Evidence trail

The persisted `(X,Z)` witness weights were:

- Python 400: `(44,40)`;
- Python 8k: `(36,34)`;
- accelerated 8k: `(34,34)`;
- accelerated 60k: `(30,34)`;
- accelerated 1M: `(30,34)` using seed 1443850366;
- all-rung consolidation: `(30,34)`.

The final claim is the witness-backed upper bound `d <= 30`, not an exact
distance.  The canonical artifact embeds explicit X-30 and Z-34 logicals.
Trusted validation passed with seed 710208008, found no lighter logical in
8,000 RIS trials, found no exact or WL-equivalent board entry, and labels the
code as advancing weight-6 x unrestricted.  Literature novelty is unverified.

- Campaign FOM: `k^2*d/n = 3.0476`.
- Board FOM: `k*d^2/n = 11.4286`.
- Canonical artifact: `c0045118-abd6897911-grid-final-allrungs-s1818095604.json`.

## Dead ends

The 400-trial estimate of 40 fell to 34 at 8k and to 30 at 60k.  The 1M rung
found no lighter witness.  Exact-grid sibling c45110 collapsed to
`[[504,16,4]]`, demonstrating that parent distance was not transferred by
assumption.

## Tools

Codex agent harness; periodic-BB constructor, deterministic campaign runner,
submission packager, `gf2_fast`, trusted Python witness checks, and the local
trusted validator.  The ladder used Python 400 and fresh Python 8k plus 8k,
60k, and 1M accelerated trials.

## Reproduction

Build on `Z_15 x Z_21` with:

```text
A = {(0,2), (0,4), (3,0)}
B = {(0,6), (7,0), (14,0)}
HX = [A | B], HZ = [B^T | A^T]
```

Candidate ID c45118; canonical key
`abd689791187bd0a514d20f2`.  The canonical all-rung document and verdict are
under `research/campaigns/2026-08-06-periodic-bb/artifacts/candidates/`.
