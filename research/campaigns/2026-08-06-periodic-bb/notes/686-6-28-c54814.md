# [[686,6,28]] — locally dominated reserve BB mutation c54814

## Direction & hypothesis

Apply a one-term local mutation to the trusted `[[686,6,36]]` parent on
`Z_49 x Z_7`, testing whether the parent distance survives a minimal support
change at fixed encoded dimension. Candidate c54814 is connected and advances
weight-6 × unrestricted, but is locally dominated. Its distance is a
witness-backed upper bound; literature novelty is unverified.

## What was searched

Epoch 4 covered IDs 54663--57662: 3,000 deterministic proposals, 369 new
encoders, and 345 connected records. A disjoint 12-code reserve queue retained
additional connected children of the highest-distance parents. c54814 uses
`validated-highd-local-one`, canonical key `5e5bccb34b76b6fa3678478f`, and
proposal seed `3122447982394748574`.

## Evidence trail

The cumulative persisted `(dX,dZ)` witnesses were `(36,36)` at 400, `(32,30)`
at 8k, `(30,30)` at 60k, and `(28,30)` at 1M. All-rung consolidation retained
the ladder `36→30→30→28`, supporting `d<=28`, not an exact distance. Trusted
validation passed with refutation seed `1149599998` and marked the code
board-advancing. `k^2*d/n=1.47`; board FOM `k*d^2/n=6.86`. The support is
locally dominated by the smaller `[[576,12,28]]` campaign point. Final
artifact:
`c0054814-5e5bccb34b-epoch4-reserve-final-allrungs-s981309440.json`.

## Dead ends

The minimal support change lost eight units of witnessed distance relative to
the `[[686,6,36]]` parent, with the final drop from 30 to 28 appearing only at
1M. The result does not improve the local parameter frontier, but records a
useful negative calibration for the high-distance `Z_49 x Z_7` lineage.

## Tools

Codex agent harness, the periodic-BB constructor, persistence-first campaign
packaging, `gf2_fast` (`pair_depth=1`, four threads), Python 400/8k witness
searches, accelerated 8k/60k/1M rungs, all-rung consolidation, and the pinned
trusted validator.

## Reproduction

Build on `Z_49 x Z_7` with `A=[(2,4),(8,2),(11,1)]` and
`B=[(3,0),(6,3),(7,5)]`; candidate c54814 uses
`validated-highd-local-one` with proposal seed `3122447982394748574`.
