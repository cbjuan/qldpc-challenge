# [[260,26,6]] — weight-6 BB geometry transfer on a 10x13 torus

## Direction & hypothesis

Probe nearby torus dimensions around the local `[[208,26,6]]` seed to learn
which geometry changes preserve its high-rate weight-6 branch and which improve
distance.

## What was searched

The campaign followed its initial 20,000 arbitrary-support proposals with
20,000 targeted mutations of witnessed and known BB parents. Candidate 20623
is a connected nearby-geometry record from the 32-code promotion batch. It
keeps the seed's 4+2 supports and changes `Z_8 x Z_13` to `Z_10 x Z_13`; its
first persisted witness package used 400 Python trials.

## Evidence trail

Both logical sides were witnessed at weight 6 at 400 trials, and the estimate
remained 6 through the saved 8k and 60k rungs. The final rung used 8,000 Python
trials at seed 533806105 and 60,000 accelerated trials at seed 533806122; its
weight-6 X witness passed the Python GF(2) checks. Trusted validation at seed
763309077 passed, found no lighter logical in its independent 8,000-trial pass,
found no exact or WL board match, and labeled the candidate board-advancing.

Claim: witness-backed `d <= 6`, not exact. Literature novelty unverified.

FOMs: `k*d^2/n = 26*36/260 = 3.60`; `k^2*d/n = 676*6/260 = 15.60`.

## Dead ends

This code advances the current public board, but it is locally dominated by
candidate 20918, `[[234,26,7]]`: that staged code is shorter, has the same
logical dimension, and has a higher witnessed distance. The `10x13` result is
retained as geometry-scaling evidence rather than a local frontier leader.

## Tools

The local campaign runner, `research/kit/bb.py`, the persistence-first
submission path, `gf2_fast` with pair depth 16, trusted Python GF(2) checks,
and `verify/validate_candidate.py`.

## Reproduction

On `Z_10 x Z_13`, use
`A={(0,0),(0,7),(2,7),(5,0)}` and `B={(0,0),(1,0)}`.
Candidate ID 20623; the final submission, raw evidence, and validator verdict
are under `../artifacts/candidates/`.
